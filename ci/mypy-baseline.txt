3356
# AUD5-PRAC-5. Первое число файла — baseline, остальное игнорируется парсером
# (ci/mypy_ratchet.py читает .split()[0]).
#
# Замер 2026-07-26, mypy==1.7.1, команда ровно как в CI-джобе `mypy`:
#   mypy --ignore-missing-imports --explicit-package-bases \
#        --exclude '(/tests/|/venv/)' uk_management_bot
# → Found 3356 errors in 168 files (checked 341 source files)
#
# В бэклоге на 2026-07-14 стояло 3118 — за 12 дней +238 приехали незаметно
# именно потому, что джоба была advisory без гейта на число. Это и есть причина
# ratchet'а.
#
# Правила: растить это число только осознанно и с объяснением в коммите;
# опускать — как только счётчик упал (гейт напомнит в логе, но CI не покраснит).
# Число зависит от версии mypy: при апгрейде пина замерить заново тем же шагом,
# не «подгонять» задним числом.
