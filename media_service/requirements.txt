# FastAPI и основные зависимости
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
pydantic-settings>=2.0.0

# База данных
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0

# Telegram Bot API
aiogram>=3.0.0

# HTTP клиент
httpx>=0.25.0
aiofiles>=23.0.0

# Валидация и обработка файлов
# AUD5-PRAC-2: пин 0.0.20 нёс 6 CVE (PYSEC-2026-1852/3036/3037/3038/3039/3040) —
# все в парсере multipart, то есть ровно на горячем пути этого сервиса (приём
# загрузок). Корневой requirements.txt был бампнут ещё в REG-03, media остался
# позади, потому что его вообще никто не сканировал.
python-multipart>=0.0.32
pillow>=11.0.0

# Логирование и мониторинг
structlog>=23.1.0
prometheus-client>=0.18.0

# Тестирование
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Безопасность
# python-jose удалён (AUD5-PRAC-2): ни одного импорта во всём сервисе —
# аутентификация здесь на API-ключах (`MEDIA_API_KEYS`), JWT сервис не трогает.
# Он же был единственным источником `ecdsa` с PYSEC-2026-1325 (Minerva, timing).
# Удалить неиспользуемый пакет строго лучше, чем внести CVE в ignore-лист:
# ignore пришлось бы поддерживать вечно, а поверхность осталась бы в образе.
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0

# Утилиты
redis>=5.0.0
celery>=5.3.0
# HTTP client для healthcheck
requests>=2.31.0
