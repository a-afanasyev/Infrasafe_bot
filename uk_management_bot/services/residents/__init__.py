"""Сервис-слой домена «Жители» (веб-администрирование жителей ЖК).

Разделение как в `services/addresses/`:
  * `queries.py`    — все чтения (список, счётчики, карточка);
  * `exceptions.py` — доменные исключения, которые API мапит в 404/409/422.
"""
