"""FSM исправления ФИО менеджером — общий для карточки жителя и сотрудника.

Состояние одно на оба раздела: отличается только откуда пришли, и это лежит в
данных FSM (`origin`), а не в отдельном состоянии.
"""

from aiogram.fsm.state import State, StatesGroup


class UserRenameStates(StatesGroup):
    """Состояния исправления ФИО чужого профиля."""

    waiting_for_full_name = State()
    """Ожидание нового ФИО одной строкой"""
