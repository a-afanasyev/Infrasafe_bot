"""
FSM состояния для экрана «Автоменеджер» (менеджеры/админы).

Минимальный набор — статус-экран не требует собственного состояния (рендерится
и обновляется прямым edit_text по callback'ам), FSM нужен только на время
текстового ввода нового окна работы (HH:MM-HH:MM).
"""

from aiogram.fsm.state import State, StatesGroup


class AutoManagerStates(StatesGroup):
    """Состояния экрана автоматического менеджера."""

    # Ожидание текстового ввода нового окна работы в формате HH:MM-HH:MM
    entering_window = State()
