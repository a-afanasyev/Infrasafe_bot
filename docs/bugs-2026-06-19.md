# QA-прогон прода 2026-06-19 (после Волны 4 + PR-31)

> **✅ Статус на 2026-07-01 (actuality pass #3):** все 3 найденных дефекта ЗАКРЫТЫ.
> **BUG-QA-01** (мёртвая кнопка `my_statistics`) — fixed, `keyboards/my_shifts.py:16` (кнопка убрана).
> **BUG-QA-02** (`recommendation_engine` executor_id→user_id) — fixed, `services/recommendation_engine.py:427`.
> **BUG-QA-03** (`document.title` на /feedback, /resident-board) — fixed, `FeedbackPage.tsx:55` + `ResidentBoardPage.tsx:71` зовут `usePageTitle`.
> Известный shift_scheduler tz-naive также fixed (`utils/shift_scheduler.py:338`, tz-aware now).

Полное функциональное тестирование бота (обе роли) + веб-дашборда (все страницы) + API на проде `95.46.96.105` (main `0576333`, alembic 022). Найденные дефекты — **все pre-existing** (не относятся к Волне 4); деплой Волны 4 чист.

## Бот (telegram-qa, infrasafebot, аккаунт executor+manager)

### PASS
- **Manager**: /start меню, Админ панель, заявки Новые/Активные/Исполненные/Закуп/Архив + детальная карточка заявки с полным набором workflow-действий, Справочник адресов (статистика/здания/квартиры), Управление пользователями (счётчики + список одобренных), Управление сотрудниками (счётчики Исполнители 5/Менеджеры 3 + список), Создать приглашение (FSM), Смены→Аналитика→Недельный отчёт, переключение роли.
- **Executor**: меню, Активные заявки, Свободные заявки (групповой пул), Архив, Смена (Моя смена/История смен с фильтрами), Мои смены (панель), Помощь, Обратная связь (FSM).
- Резолв ролей PR-31 (без legacy-колонки) + `legacy_role_filter` (roles-contains) — вживую корректны.

### Дефекты
- **BUG-QA-01 (P2)**: «📋 Мои смены → 📈 Моя статистика» — **мёртвая кнопка**. `keyboards/my_shifts.py:21` задаёт `callback_data="my_statistics"`, но обработчика нет (grep `my_statistics` → только клавиатура). Клик молча игнорируется (нет ни лога, ни ответа). Соседние кнопки (Текущие смены/Учёт времени/Передача смен/Расписание) — обработчики есть.
- **BUG-QA-02 (P2)**: «Смены → Аналитика → Рекомендации по оптимизации» — крах. `services/recommendation_engine.py:427,479` читает `shift.executor_id`, но модель `Shift` использует `user_id` → лог `ERROR ... 'Shift' object has no attribute 'executor_id'`. Фича оптимизационных рекомендаций нерабочая.

### Известные (зафиксированы ранее)
- BUG-BOT-001 (P3): `BOT_USERNAME` не задан в прод `.env` → fallback к token-derived `infrasafebot` (ERROR-лог при старте, функционально не влияет).
- shift_scheduler (P2): уведомления о сменах падают `can't subtract offset-naive and offset-aware datetimes` (tz-naive vs aware).

## Веб-дашборд (chrome-devtools, manager, https://infrasafe.uz/uk/)

### PASS (консоль без ошибок на всех страницах)
- **Заявки/Kanban**: все колонки статусов + реальные заявки (маркеры 🤖 auto-dispatch / 📞 call-center, срочность, badges).
- **Аналитика**: KPI, графики по дням/категориям/статусам, топ исполнителей, последние действия.
- **Сотрудники**: 5 карточек, статистика, фильтры роль/статус/спец, действия.
- **Смены**: KPI покрытия, расписание, шаблоны, тепловая карта, табы день/неделя/месяц.
- **Шаблоны**: 3 шаблона (циклы/дни/спец/действия).
- **Адреса** (PR-33): 3-уровневый drill-down Двор→Здание→Квартира + крошки/статистика/табы (квартира с `area`=122 м² — PR-34 Numeric end-to-end).
- **Редактор витрины**: блоки (drag+checkbox), УК RU/UZ, контакты, бот, объявления, часы работы + live-превью.
- **Обратная связь**: фильтры тип/статус + список обращений.
- **Табло жителей** (публичное): объявления, часы, статистика, заявки по статусам, real-time.
- Все address-API (PR-28) и shifts-API (PR-27) → 200.

### Дефекты
- **BUG-QA-03 (P3)**: `document.title` не обновляется на части страниц (остаётся от предыдущей: на `/feedback` и `/resident-board` показывает «Редактор витрины — UK Management»). Причина — не все страницы вызывают `usePageTitle`. Косметика.

## API
- `/health`, `/api/health` → 200. `/api/health/outbox`, `/api/health/ratelimit` → 401 (защищены HEALTH_METRICS_TOKEN — ожидаемо).
- 104 роута, без startup-ошибок.

## Вывод
Деплой Волны 4 + PR-31 функционально чист (0 регрессий). Найдено 3 pre-existing дефекта (2×P2 бот, 1×P3 веб) + 2 известных. Кандидаты на отдельные фиксы — приоритет за владельцем.
