# Бэклог по результатам аудита — 2026-05-20

**Источник:** 7 параллельных агентов + pytest + vitest + Playwright E2E.
**Базовый отчёт:** `docs/audit/2026-05-20-full-review.md`.
**Всего entries (актуально на 2026-05-23):** 164 Priority-помеченных (+OPS-112, +ARCH-113/114, +SEC-115 integration review, +TEST-116, +OPS-117 FIX-007 build, +FE-119 FIX-007 cutover), из них 3 merged-маркера (cross-ref дубли) + 1 omitted = **161 effective active** + 3 cross-ref + 1 omitted = **165 entries в файле**.
**Status split:** **39 merged в `origin/main`** (33 BUG-BOT + 4 FIX-001/003/005/006 через PR #17..#20 от 2026-05-21 + ARCH-014 через PR #23 от 2026-05-22, поглотил ARCH-016+ARCH-102 + FIX-007 через PR #24 от 2026-05-22, поглотил TEST-116). **2 dev-closed, prod-pending** (FIX-002 dev ротация, FIX-004 dev applied — все 2026-05-21). **2 awaiting-user-action prod** (FIX-002 prod, FIX-004 prod). **FIX-007 merged** (P0 release-gate закрыт), **FIX-008 downgraded** P0→P1. **Open (other):** ~117. См. **Closed Items Registry** ниже и **Итоговая статистика после patch** в конце для полного breakdown.
**Verification:** cross-check агентом подтверждено покрытие всех CRITICAL/HIGH; патч применён по 3 missing items (DB-103, DB-104, OPS-105) + сводка дубликатов и второй патч по 7 редакционным замечаниям внизу.
**Соглашения:**
- **Priority**: P0 (блокер релиза) / P1 (фиксить в этом спринте) / P2 (некритично, плановое) / P3 (nice-to-have).
- **Type** (полный список, расширен по факту использования): BUG / SEC / PERF / ARCH / REFACTOR / DOCS / TEST / UX / I18N / DB / OPS / INV (investigation).
- **ID-prefix** (FIX / ARCH / SEC / BUG / REFACTOR / FE / DB / TEST / NICE / DOCS / INV / OPS / PERF): отражает **исторический контекст обнаружения**, а не Type. Пример: `BUG-090` имеет Type `PERF`, потому что был зарегистрирован в общем потоке "багов" агента, но по природе это performance-задача. Источник истины для классификации — поле `Type`.
- **ID numbering**: хронологический (по моменту обнаружения), не приоритетный. После initial-набора добавлялись патчи (SEC-083..086, SEC-092, SEC-096, REFACTOR-088..091, BUG-090, BUG-097, INV-087, INV-098, PERF-093, FE-094..100, DOCS-095, ARCH-101..102, DB-103..104, OPS-105) — отсюда не-монотонность. Сортировка для работы — по `Priority` (см. секции P0/P1/P2/P3), не по ID.
- **Source**: какой агент нашёл (A=architect, S=security, P=python, T=typescript, D=database, Q=qa, E=e2e).
- **Status**: open везде, кроме отдельно помеченных.
- **AC (Acceptance Criteria)** обязательны для P0/P1; для P2/P3 — опциональны (фикс/тип/файл считается достаточным, формальные AC формулируются при взятии задачи в работу).

---

## Closed Items Registry (last updated 2026-05-21)

**Quick lookup of resolved items.** Detailed entries (with full description and AC) остаются в соответствующих P0/P1/P2/P3 секциях ниже. Этот регистр — single-source-of-truth для статуса.

### ✅ Closed and merged to `origin/main` (33 items)

P0+P1 batch (commit `293b060`):

| ID | Prio | Fix commit | Branch (deleted) |
|---|---|---|---|
| BUG-BOT-001 | P0 | `0076d41` (+ `0703a7d` env-template + `d415747` logger compat patch) | fix/bug-bot-001-bot-username |
| BUG-BOT-002 | P1 | `504a0df` | fix/bug-bot-002-006-shift-transfer |
| BUG-BOT-003 | P1 | `5ca2b20` | fix/bug-bot-003-004-005-permissions |
| BUG-BOT-004 | P1 | `86ce32c` | (same branch) |
| BUG-BOT-005 | P1 | `fb4ce51` | (same branch) |
| BUG-BOT-006 | P1 | `75a1797` | fix/bug-bot-002-006-shift-transfer |
| BUG-BOT-018 | P1 | `1ce12fd` (+ `6bb9853` cancel routing exclusion) | fix/bug-bot-018-shift-plan-confirm |
| BUG-BOT-019 | P1 | `c648b19` | fix/bug-bot-019-archive-routing |

P2 batch (commit `6e6f28e`):

| ID | Prio | Fix commit | Branch (deleted) |
|---|---|---|---|
| BUG-BOT-007 | P2 | `5019700` | fix/bug-bot-p2-errors-data-batch |
| BUG-BOT-008 | P2 | `4d5685c` | fix/bug-bot-p2-l10n-batch |
| BUG-BOT-009 | P2 | `e6fc96d` | (same) |
| BUG-BOT-010 | P2 | `f7a1588` | (same) |
| BUG-BOT-011 | P2 | `950806a` | (same) |
| BUG-BOT-012 | P2 | `443cda7` | (same) |
| BUG-BOT-013 | P2 | `3cdfa4a` | fix/bug-bot-p2-errors-data-batch |
| BUG-BOT-014 | P2 | `697a3d8` | fix/bug-bot-p2-nav-ux-batch |
| BUG-BOT-020 | P2 | `d106c3b` | (same) |
| BUG-BOT-021 | P2 | `3e8b0dc` | (same) |
| BUG-BOT-022 | P2 | `72ea1dc` | fix/bug-bot-p2-errors-data-batch |
| BUG-BOT-023 | P2 | `6a74251` | fix/bug-bot-p2-l10n-batch |
| BUG-BOT-025 | P2 | `4954c7b` | fix/bug-bot-p2-nav-ux-batch |
| BUG-BOT-028 | P2 | `64fbe52` | fix/bug-bot-p2-errors-data-batch — **partial fix; raised BUG-BOT-031/032 as follow-ups** |

P3 + BUG-BOT-031 batch (commit `913ba9d`):

| ID | Prio | Fix commit | Branch (deleted) |
|---|---|---|---|
| BUG-BOT-015 | P3 | `cbad970` | fix/bug-bot-p3-batch |
| BUG-BOT-016 | P3 | `70d3b76` | (same) |
| BUG-BOT-024 | P3 | `7cc1cdc` | (same) |
| BUG-BOT-026 | P3 | `57bd920` | (same) |
| BUG-BOT-027 | P3 | `74d1e6d` | (same) |
| BUG-BOT-029 | P3 | `c4b34cb` | (same) |
| BUG-BOT-030 | P3→P1 | `2f828d8` | (same) |
| BUG-BOT-031 | P2 | `e9c58d1` | fix/bug-bot-031-metrics-manager |

Late discoveries fixed in-session:

| ID | Prio | Fix commit | Branch (deleted) |
|---|---|---|---|
| BUG-BOT-032 | P2 | `4065c57` (merge `99408e7`) | fix/bug-bot-032-format-specs |
| BUG-BOT-033 | P1 | `7c1ba4c` (merge `27b4548`) | fix/bug-bot-033-view-prefix-collision |

### ✅ FIX-00X batch merged to `origin/main` (4 items — 2026-05-21 late session)

| ID | Prio | PR | Merge commit | Branch (deleted) |
|---|---|---|---|---|
| FIX-001 | P0 | [#17](https://github.com/a-afanasyev/Infrasafe_bot/pull/17) | `164b2ad` | fix/fix-001-invite-namerror |
| FIX-003 | P0 | [#18](https://github.com/a-afanasyev/Infrasafe_bot/pull/18) | `dd3ea52` | fix/fix-003-fk-purge-set-null |
| FIX-005 | P0 | [#19](https://github.com/a-afanasyev/Infrasafe_bot/pull/19) | `080d824` | fix/fix-005-webhook-503-retryable |
| FIX-006 | P0 | [#20](https://github.com/a-afanasyev/Infrasafe_bot/pull/20) | `f45f5aa` | fix/fix-006-invite-token-log-mask |

Each branch rebased on top of post-BUG-BOT main, then merged via merge-commit (no squash). After PRs landed, all 4 bugs are gone from `origin/main`.

### ⏸ Awaiting user action (prod only)

| ID | Prio | What's needed |
|---|---|---|
| FIX-002 prod | P0 | Same rotation as dev, but on **production** `.env` файлах. Dev applied 2026-05-21 (BOT_TOKEN/MEDIA_BOT_TOKEN revoked, INVITE/JWT/UK_WEBHOOK/INFRASAFE_WEBHOOK/ADMIN_PASSWORD rotated, INFRASAFE coordinated switch с operator done). Runbook: `docs/audit/runbooks/FIX-002.md` |
| FIX-004 prod | P0 | ALTER ROLE uk_bot NOSUPERUSER on **production** (dev applied 2026-05-21; Step 6 NOLOGIN-hardening deferred per user). Runbook: `docs/audit/runbooks/FIX-004.md` |

### 📋 Open BUG-BOT items (data cleanup / tech-debt)

| ID | Prio | Note |
|---|---|---|
| BUG-BOT-017 | P3 | Junk shift templates (`еуые`, `test`) — manual SQL cleanup |
| BUG-BOT-034 | P3 | accept_/purchase_ prefix-match anti-pattern (tech-debt, low risk) |
| BUG-BOT-035 | P3 | cancel_clarification edge-case in handle_manager_back_to_list |

### ✅ ARCH-014 merged (2026-05-22) — gating снято

| ID | Prio | Note |
|---|---|---|
| ARCH-014 | P1 | Unify sync AddressService + async router. **Merged** PR #23 `732e365`. Поглотил ARCH-016 + ARCH-102. |
| ARCH-016 | P1 | Single EventBus — closed, merged-into ARCH-014 (`services/addresses/events.py`) |
| ARCH-102 | P3 | Engine drift — closed, merged-into ARCH-014 |
| FIX-007 | P0 | ✅ Merged — inbound HMAC webhook (Фазы 1+2), PR #24 `1a95c00`, post-merge ревью-фиксы применены |
| FIX-008 | P1 | Downgraded P0→P1 — de-async read methods, отдельный ticket (D-FIX008, out of ARCH-014 scope) |

### Aggregate

- **BUG-BOT-* merged to `origin/main`:** 33 (001..016, 018..033 кроме 017)
- **FIX-00X merged to `origin/main`:** 4 (FIX-001 PR #17, FIX-003 PR #18, FIX-005 PR #19, FIX-006 PR #20 — все 2026-05-21)
- **Awaiting user-action:** 2 (FIX-002 secret rotation, FIX-004 prod ALTER ROLE)
- **Gated by ARCH-014:** 2 (FIX-007, FIX-008)
- **Open BUG-BOT-* items:** 5 — P3: 017 (data), 034, 035, 039, 040 (всё P3 newly-filed)
- **Newly filed open in P1/P2:** BUG-BOT-036 (P1), 037, 038 (P2) — нет fix-коммитов
- **ARCH-014 merged (2026-05-22, PR #23 `732e365`):** поглотил ARCH-016 + ARCH-102 (оба closed); FIX-007 ungated; FIX-008 downgraded P0→P1
- **Tests added across session:** ~182 unit tests + 28 ARCH-014 (core/events/parity), all passing
- **Smoke verifications:** ~30 menus via MCP

---

## P0 — Блокеры и release gates (8 total — 6 merged · 2 dev-closed/prod-pending · 0 open)

**Status:** 6 merged to main: BUG-BOT-001, FIX-001 (PR #17 `164b2ad`), FIX-003 (PR #18 `dd3ea52`), FIX-005 (PR #19 `080d824`), FIX-006 (PR #20 `f45f5aa`) — all merged 2026-05-21; FIX-007 (PR #24 `1a95c00`, 2026-05-22). FIX-002 dev rotated 2026-05-21 (prod — отдельный TODO). FIX-004 dev applied 2026-05-21 (uk_bot rolsuper=f×3 verified; Step 6 NOLOGIN deferred per user 2026-05-21; prod — отдельный TODO). FIX-007 — Фазы 1+2 + post-merge ревью-фиксы merged; остаётся только координация секрета через `age`. FIX-008 downgraded P0→P1.

Секция содержит **два типа P0**:

- **Hard blockers** (фиксить до прод-релиза): FIX-001 (NameError invite), FIX-002 (секреты), FIX-003 (FK purge), FIX-004 (superuser БД), FIX-005 (webhook 503), FIX-006 (токен в логах).
- **Release gates** (P0 как обязательное условие для смежной работы, допустимы к понижению с явным обоснованием): FIX-007 (inbound HMAC — gating на реализацию endpoint), FIX-008 (AddressService async без await — gating для ARCH-014).

Gating-пункты помечены в заголовке/описании самого пункта. Если parent-эпик откладывается — пункт можно понижать до P1 с явной записью в коммите.

### FIX-001 — `NameError: user_data` в invite-регистрации
- **Priority:** P0
- **Type:** BUG
- **Source:** A (C1), S (C-2), P (C1), Q (BUG-1)
- **Files:** `uk_management_bot/web/api/invite.py:121-124`
- **Description:** Переменная `user_data` не определена в ветке нового пользователя. POST `/api/register` для нового `telegram_id` → 500. Регистрация новых сотрудников по invite полностью сломана.
- **Fix:** Разбить `data.full_name.split()` на `first_name` + `" ".join(rest)`, как в ветке existing-user.
- **AC:** POST `/api/register` с новым `telegram_id` отдаёт 200 и пользователь появляется в `users` (проверяется через `curl` + `SELECT FROM users WHERE telegram_id=...`). Полный pytest-прогон `test_invite_integration` — отдельная задача TEST-069 (legacy-импорты), не входит в AC этого фикса.
- **Estimate:** 10 min.
- **Status:** ✅ **merged to main** (2026-05-21, PR [#17](https://github.com/a-afanasyev/Infrasafe_bot/pull/17), merge commit `164b2ad`, fix commit `5b8dccf`). Branch `fix/fix-001-invite-namerror` deleted. Verifier log `docs/audit/verifier-logs/FIX-001.md`.

### FIX-002 — Ротация всех живых секретов на диске
- **Priority:** P0
- **Type:** SEC
- **Source:** S (C-1)
- **Files:** `.env`, `uk_management_bot/.env`, `media_service/.env`
- **Description:** На диске в открытом виде лежат **8 значений**: `BOT_TOKEN` ×2 (разные id), `MEDIA_BOT_TOKEN`, `INVITE_SECRET`, `INFRASAFE_WEBHOOK_SECRET` (outgoing подпись), `UK_WEBHOOK_SECRET` (inbound, см. FIX-007), `JWT_SECRET`, `ADMIN_PASSWORD`. В git они не утекли (gitignored), но любой с доступом к dev-машине / образу контейнера видит plaintext.
- **Fix:** Revoke в @BotFather двух BOT_TOKEN + MEDIA_BOT_TOKEN; сгенерировать новые INVITE_SECRET / INFRASAFE_WEBHOOK_SECRET / UK_WEBHOOK_SECRET / JWT_SECRET (`openssl rand -base64 32`); перевыставить ADMIN_PASSWORD ≥16 символов; удалить дубль `MEDIA_BOT_TOKEN`, который сейчас лежит в двух файлах одновременно.
- **AC:** Все 8 значений имеют новые секреты; старые BOT-токены отозваны в @BotFather; новые значения в менеджере секретов или `.env` (никогда не в git); `grep -rE "^(BOT_TOKEN|JWT_SECRET|INVITE_SECRET|.*WEBHOOK_SECRET|ADMIN_PASSWORD)=" .env*` показывает только новые значения.
- **Estimate:** 30 min.
- **Status:** ✅ **dev rotated** (2026-05-21). Все 7 секретов в dev `.env` обновлены (BOT_TOKEN, MEDIA_BOT_TOKEN, INVITE_SECRET, JWT_SECRET, ADMIN_PASSWORD, UK_WEBHOOK_SECRET, INFRASAFE_WEBHOOK_SECRET). Unified ADMIN_PASSWORD + INVITE_SECRET + BOT_TOKEN across root `.env` и `uk_management_bot/.env`. UK_WEBHOOK_SECRET добавлен в root `.env` (был только в `settings.py` default). INFRASAFE_WEBHOOK_SECRET: dual-secret flow (NEXT staged → flip USE_NEXT=true → coordinated promote с operator → primary). Бэкап `.env`: `~/.uk-secret-backups/20260521T123205Z/*.env` (хранится 7 дней). Temp файлы со значениями shred'нуты. Все 6 контейнеров healthy после rotation. **Prod часть** — отдельный TODO (FIX-002 prod, см. Awaiting user action).

### FIX-003 — `requests.apartment_id` FK блокирует purge
- **Priority:** P0
- **Type:** DB / BUG
- **Source:** A (C3), D (C2)
- **Files:** alembic миграция (новая); затронуты `apartments`, `requests`
- **Description:** FK `requests.apartment_id → apartments.id` на `ON DELETE NO ACTION`. Purge yard/building падает на FK violation при наличии исторических заявок (включая race с одновременным INSERT request).
- **Fix:** Alembic-миграция: DROP CONSTRAINT + ADD CONSTRAINT `ON DELETE SET NULL`. Альтернатива (если бизнес требует сохранять linkage) — explicit guard через trigger.
- **AC:** Удаление apartment с историческими requests не падает, `requests.apartment_id` становится NULL; покрыто тестом `test_apartment_purge_keeps_request_history`.
- **Estimate:** 1 h (миграция + тест + smoke).
- **Status:** ✅ **merged to main** (2026-05-21, PR [#18](https://github.com/a-afanasyev/Infrasafe_bot/pull/18), merge commit `dd3ea52`, fix commit `6f2eed4`). Branch `fix/fix-003-fk-purge-set-null` deleted. Migration `007_set_null_on_request_apartment_fk.py` теперь в `alembic/versions/`. Verifier log `docs/audit/verifier-logs/FIX-003.md`.

### FIX-004 — `uk_bot` — суперпользователь PostgreSQL
- **Priority:** P0
- **Type:** SEC / OPS
- **Source:** D (C1)
- **Files:** infra (psql / Alembic) — `ALTER ROLE`
- **Description:** Прикладной user имеет `rolsuper=t, rolcreatedb=t, rolcreaterole=t`. Любая SQLi или компрометация коннекшена → `DROP DATABASE`.
- **Fix:** `ALTER ROLE uk_bot NOSUPERUSER NOCREATEDB NOCREATEROLE`. Перепроверить, что миграции и приложение не используют DDL за пределами `GRANT`.
- **AC:** `SELECT rolsuper, rolcreatedb, rolcreaterole FROM pg_roles WHERE rolname='uk_bot'` возвращает три `false`. Полный прогон `alembic upgrade head` после revoke — успешный (т.е. явные `GRANT`-ы покрывают всё, что нужно приложению).
- **Estimate:** 45 min.
- **Status:** ✅ **dev applied** 2026-05-21 (re-verified 2026-05-21: uk_bot rolsuper=f, rolcreatedb=f, rolcreaterole=f). `uk_admin` replacement SUPERUSER LOGIN bootstrapped. Runbook bug found and patched: `pg_admin` is PG reserved → renamed to `uk_admin` everywhere. **Step 6 NOLOGIN-hardening: explicitly deferred by user 2026-05-21** ("оставим uk_admin LOGIN пока так") — pre-condition fails (POSTGRES_USER=uk_bot is now NOSUPERUSER, cannot recover uk_admin LOGIN without `pg_break_glass` emergency superuser which user declined to create). Current state: uk_admin LOGIN superuser, accessible for emergency operations; security boundary established by uk_bot NOSUPERUSER. ⚠ Monitoring TODO: alert on `pg_stat_activity` connections under `usename='uk_admin'` (any connection = anomaly). **Prod** — separate user-action item. Verifier log: `docs/audit/verifier-logs/FIX-004.md`

### FIX-005 — Webhook HTTP 503 помечен `retryable=False`
- **Priority:** P0
- **Type:** BUG
- **Source:** P (C3)
- **Files:** `uk_management_bot/services/webhook_sender.py:189-190`
- **Description:** По коду `process_outbox` (lines 250-330) переход в `failed` срабатывает по условию `if not retryable or record.attempts >= max_retries`. То есть при `retryable=False` запись становится `failed` **сразу после первой неудачи**, не "после max_retries". Короткий 503 → перманентная потеря события.
- **Fix:** `return (False, "HTTP 503: service unavailable", True, 0)`.
- **AC:** Юнит-тест: при `httpx.Response(status_code=503)` запись после одного цикла `process_outbox` имеет `status="pending"`, `attempts=1` (поле модели — `attempts`, не `retry_count`), `retry_after` установлен; уходит в `failed` только после `attempts >= settings.INFRASAFE_WEBHOOK_MAX_RETRIES`.
- **Estimate:** 15 min.
- **Status:** ✅ **merged to main** (2026-05-21, PR [#19](https://github.com/a-afanasyev/Infrasafe_bot/pull/19), merge commit `080d824`, fix commit `b8d142e`). Branch `fix/fix-005-webhook-503-retryable` deleted. Verifier log `docs/audit/verifier-logs/FIX-005.md`.

### FIX-006 — Invite-токен в plaintext в логах
- **Priority:** P0
- **Type:** SEC
- **Source:** S (C-4)
- **Files:** `uk_management_bot/handlers/auth.py:172`
- **Description:** Полный invite-токен (TTL — часы/дни) пишется в info-логи. Тот, у кого доступ к `docker logs`, может зарегистрироваться по чужому токену в окне TTL.
- **Fix:** Маскировать как `f"{token[:8]}…"` — оставить только префикс, без хвоста, без длины. (Согласовано с full-review §6: дополнительный материал в хвосте увеличивает рейнбоу-поверхность.) Аналогичный аудит других logger-строк (см. REFACTOR-032).
- **AC:** `docker logs uk-management-bot 2>&1 | grep -E "invite_v1:[A-Za-z0-9_-]{20,}"` → пусто.
- **Estimate:** 30 min.
- **Status:** ✅ **merged to main** (2026-05-21, PR [#20](https://github.com/a-afanasyev/Infrasafe_bot/pull/20), merge commit `f45f5aa`, fix commit `bb52ffc`). Branch `fix/fix-006-invite-token-log-mask` deleted. Coordinator extended scope to mask BOTH entry log (line 66) and exit log (line 172) for AC compliance. Verifier log `docs/audit/verifier-logs/FIX-006.md`.

### FIX-007 — Inbound webhook router без HMAC verification — **gating-условие, не runtime-блокер**
- **Priority:** P0 (gating)
- **Type:** SEC / ARCH
- **Source:** S (C-3)
- **Files:** `uk_management_bot/config/settings.py:99-100` (secret уже выдан) + новый роутер
- **Description:** `UK_WEBHOOK_SECRET` объявлен в settings, но endpoint-приёмник от InfraSafe не реализован. Текущая поверхность атаки = 0 (обращение → 404). Пункт P0 как gate: реализация inbound должна **с первого коммита** включать HMAC-проверку, иначе появится временное окно без защиты. Также: значение `UK_WEBHOOK_SECRET` уже лежит на диске и должно быть ротировано вместе с остальными секретами (FIX-002).
- **Fix:** При реализации inbound endpoint — verify HMAC SHA-256 над сырым body перед обработкой; rotation через `UK_WEBHOOK_SECRET_NEXT`; replay protection через nonce/timestamp; rate-limit. Если до релиза inbound не делается — пункт допустимо понизить до P1 и снять с критического пути.
- **AC:** Endpoint без подписи → 401; с неверной подписью → 401; replay (тот же nonce 2 раза) → 409.
- **Estimate:** 4 h.
- **Status:** ✅ **Фазы 1 + 2 реализованы, ревью пройдено, merged в `main`** (2026-05-22, PR #24 `1a95c00` + post-merge ревью-фиксы `abb8c58`).
  - **Фаза 1** (security envelope, commit `1ea71dc`): пакет `api/webhooks/` (HMAC verify над raw body, dual-secret, constant-time; Redis replay `SET NX EX`; rate-limit 60/min; audit-log), `POST /api/v2/webhooks/infrasafe/alert`.
  - **Фаза 2** (handler alert→request, commit `4cd0cea`): `services/inbound_alert.py` — резолв building по `external_id` (full-scan), маппинг `type→category`/`severity→urgency` (`api/webhooks/mappings.py`), создание building-level заявки от системного юзера, emit `request.created` с `source_event_id`, durable dedup+audit в `webhook_inbox`. Миграции `008` (webhook_inbox) + `009` (системный юзер) применены. Doc-fix handoff `2597078`.
  - **Post-merge код-ревью (2026-05-22, агент `code-reviewer`):** 0 CRITICAL, 3 HIGH, 3 MEDIUM, 3 LOW. Применено 5 фиксов прямым коммитом в `main`: (1) HIGH — результат `is_replay()` отбрасывался, Redis-guard был мёртвым кодом → теперь возвращает 409; (2) HIGH — `db.rollback()` в `_create_request` откатывал всю сессию → `SAVEPOINT` через `begin_nested()`; (3) HIGH — отсутствие миграции 009 → unhandled 500 → ловится, 503; (4) MEDIUM — `event_id` без `max_length` → `Field(max_length=64)`; (5) LOW — `created_at=""` в исходящем `request.created` убран. 3 косметических замечания не приняты (обоснование в diff).
  - **Тесты:** FIX-007 + схемы — 54/54 зелёные. `tests/api/` целиком — 312 passed, 1 fail (`test_tampered_token_returns_none` — проблема изоляции тестов, не регрессия FIX-007). Полный `tests/` — 10 ошибок коллекции у legacy top-level тестов (устаревшие импорты `services.X`, pre-existing — см. TEST-069).
  - **Live-smoke (прод-окружение):** `alert.created` → 202 + `request_number`; replay → 409 + тот же номер; заявка в БД (category/urgency смаплены, apartment_id=NULL, source=infrasafe); `webhook_inbox`/`webhook_outbox` корректны.
  - **AC бэклога (401/401/409) выполнен. P0 release-gate закрыт.** Контракт UK↔InfraSafe согласован (раунды A-Q).
  - **Остаётся (вне кода):** координация секрета с InfraSafe через `age`; сообщить InfraSafe Phase 2 + ARCH-113 ETA. **ARCH-113** — отдельная зависимость для полноты счётчиков заявок InfraSafe (не блокирует FIX-007).
  - Артефакты: планы `docs/audit/2026-05-22-FIX-007-plan.md` + `~/.claude/plans/`, handoff `...-FIX-007-infrasafe-operator-handoff.md`, контракт `Infrasafe/docs/audit/2026-05-22-FIX-007-uk-integration-questions.md`.

### FIX-008 — AddressService — 16 `async def` без `await` на sync Session — **tech-debt, не runtime-блокер**
- **Priority:** ~~P0 (gating)~~ → **P1** (downgraded 2026-05-22 — ARCH-014 merged, перестал быть gating)
- **Type:** ARCH / REFACTOR
- **Source:** P (C4)
- **Files:** `uk_management_bot/services/address_service.py` (lines 23, 68, 75, 249, 263, 545, 561, 586, 780, 815, 850, 870, 893, 914, 944, 1036)
- **Description:** Методы помечены `async def`, принимают sync `Session`, не содержат ни одного `await`. **Уточнение по факту:** `grep "AddressService" uk_management_bot/api/` пуст — FastAPI/API не вызывает сервис напрямую. Путь использования — только sync aiogram-handlers (bot). Текущий event loop FastAPI не блокируется. Однако комбинация `async def` + sync `Session` — ловушка: первый же роутер, который попробует `await AddressService.method(...)`, получит тихое блокирующее поведение.
- **Fix:** Решение 1 — убрать `async`, оставить sync helper для bot-handler'ов (минимальная инвазивность, фиксирует контракт). Решение 2 (отдельный эпик) — переписать на `AsyncSession` + `await db.execute(...)`; не входит в этот фикс, помечено в "Что не покрыто бэклогом".
- **AC:** Каждый метод либо имеет хотя бы один `await`, либо объявлен без `async`. Mypy/`grep "async def .* db: Session"` в файле не находит совпадений. (Перформанс-бенчмарк до/после в этой задаче не делается — нагрузочного path через async-вызов сейчас нет.)
- **Estimate:** 2 h (решение 1).
- **Note по приоритету:** Был P0-gating для ARCH-014. ARCH-014 merged 2026-05-22 с решением D-FIX008 — FIX-008 (de-async 12 read-методов + 37 handler await-sites) сознательно вынесен **из scope** эпика. После ARCH-014 write-методы async по-настоящему (адаптер над async core); остаются только read-методы как `async def` без await. FIX-008 теперь plain P1 refactor, отдельный ticket.

---

## P1 — В этот спринт (43 total — 11 closed, 32 open)

**Status:** 11 closed — 8 BUG-BOT (002/003/004/005/006/018/019/033, merged commit `293b060`/`27b4548`) + ARCH-014 + ARCH-016 (PR #23 `732e365`, 2026-05-22) + ARCH-011 (resolved by `a20686f`, downgraded P1→P3). 4 newly filed (BUG-BOT-036 — notify_user asyncio.run в running loop; OPS-112 — webhook-hardening prod-verification; ARCH-113 + ARCH-114 — integration review 2026-05-22). FIX-008 переехал сюда из P0 (downgrade 2026-05-22). 29 open от initial audit waiting prioritisation.

Note: счётчик включает все P1 в файле, в том числе SEC-096, который физически расположен в нижней секции "Дополнения" (см. примечание там). Сортировка для работы — по полю `Priority`, не по принадлежности секции.

### Architecture

#### ARCH-009 — Два FastAPI app с расходящимися конфигами
- **Priority:** P1 — **Type:** ARCH — **Source:** A (H1)
- **Files:** `uk_management_bot/api/main.py`, `uk_management_bot/web/main.py`
- **Description:** Независимые CORS, middleware, Sentry-init. `web/main.py` не выключает docs в проде.
- **Fix:** Либо слить под одну /api/v2/, либо изолировать строго: `web` вызывает `api` через HTTP. Минимум — общая `security_headers_middleware` фабрика. **Включает sub-tasks:** ARCH-101 (docs/openapi disable в web/main.py) и NICE-080 (Sentry init в web).
- **AC:** Один источник истины для CORS allowlist; sentry-init shared; docs OFF в проде в обоих.
- **Estimate:** 1 d.

#### ARCH-010 — Outbox: недетерминированный `event_id`
- **Priority:** P1 — **Type:** ARCH — **Source:** A (H3)
- **Files:** `uk_management_bot/services/webhook_sender.py`
- **Description:** `event_id = uuid.uuid4()` per-call. Два разных вызова `queue_webhook` для одного entity (например, ручной retry оператора + reconciler через час) получают разные id → receiver не может сам отбросить дубликат без отдельной storage-логики (entity_id + updated_at сравнения).
- **Fix:** `event_id = sha256(f"{event}:{entity_id}:{entity_updated_at}")[:32]`. Это даёт идемпотентность по природе: один и тот же логический момент = один id. Receiver-side dedup (если он есть) продолжает работать; отсутствие receiver-side dedup перестаёт быть критичным.
- **AC:** Два вызова `queue_webhook(building.updated, building_id=X)` без изменения `updated_at` дают одинаковый `event_id`; после смены `updated_at` — разный.
- **Estimate:** 1 h.

#### ARCH-011 — Reconciliation cap=50 sort by hash
- **Priority:** ~~P1~~ → **P3** (downgraded 2026-05-22 — correctness-баг устранён, остаток косметический)
- **Type:** BUG / ARCH — **Source:** A (H4)
- **Files:** `uk_management_bot/services/reconciliation.py`
- **Description:** Сортировка по external_id (хеш) — при расхождении >50 одни и те же 50 хешей replay'ятся бесконечно.
- **Fix:** Сортировать по `created_at` или счётчику пропусков.
- **AC:** Юнит-тест: 100 missing → 2 цикла покрывают все 100 records.
- **Estimate:** 1 h.
- **Status:** ✅ **resolved by `a20686f`** (2026-05-19 — переписанный `reconciliation.py`, `reconcile_buildings`). Premise ARCH-011 («одни и те же 50 replay'ятся бесконечно») **больше не верна**: `missing_in_is = expected_set - is_externals` пересчитывается каждый цикл, успешно доставленные buildings уходят из set'а → следующий цикл берёт следующие 50, backlog дренируется ⌈N/50⌉ циклов. AC выполнен (`tests/services/test_reconciliation.py` существует). **Остаток (P3, optional):** `sorted(missing_in_is)[:REPLAY_CAP]` на строке 120 даёт произвольный порядок по хешу вместо oldest-first — чисто косметика, на корректность/прогресс не влияет.

#### ARCH-012 — `api/main.py` смешивает три слоя
- **Priority:** P1 — **Type:** REFACTOR — **Source:** A (H5)
- **Files:** `uk_management_bot/api/main.py`
- **Description:** Lifespan-таски + inline-endpoints (`/announcements`, `/media`) + bootstrap в одном файле.
- **Fix:** Разнести в `lifecycle.py`, `routes/announcements.py`, `routes/media_proxy.py`.
- **AC:** `api/main.py` ≤ 200 строк, держит только app init и include_router.
- **Estimate:** 3 h.

#### ARCH-013 — `next(get_db())` в bot handlers без управления lifecycle
- **Priority:** P1 — **Type:** BUG / ARCH — **Source:** A (H6)
- **Files:** `uk_management_bot/handlers/address_buildings.py:54` (и аналогичные)
- **Description:** Sync-generator без `with`/`finally` → утечка коннекшенов при исключении.
- **Fix:** Контекст-менеджер: `with SessionLocal() as db: ...`.
- **AC:** Grep `next(get_db())` → пусто.
- **Estimate:** 2 h (по всему bot/).

#### ARCH-014 — Дублирование AddressService (sync) и addresses/router.py (async)
- **Priority:** P1 — **Type:** ARCH — **Source:** A (H2), P (H1/H2)
- **Files:** `services/address_service.py`, `api/addresses/router.py`
- **Description:** Two-repo drift: разная валидация, разные счётчики, разный emit (sync queue_webhook_sync vs async queue_webhook + publish_redis).
- **Fix:** Один источник истины — async `services/addresses/core.py` (14 write-функций, primitives input). `AddressService` стал thin bot-adapter, API router вызывает core напрямую. EventBus split: `enqueue_outbox` (pre-commit) + `publish_realtime_after_commit` (post-commit) — закрывает ARCH-016.
- **AC:** Поведение CRUD-операций идентично из bot и из API.
- **Estimate:** 1 d.
- **Status:** ✅ **merged to main** (2026-05-22, PR [#23](https://github.com/a-afanasyev/Infrasafe_bot/pull/23), merge commit `732e365`; refactor `41b3ce7`, test-fix `25c7b33`). Реализовано удалённо (Ultraplan → Claude Code web), патч применён локально. Поглощает **ARCH-016** и **ARCH-102** — оба closed. Code review: 0 CRITICAL, 1 HIGH (WS `/ws/v2/buildings` `close()` без `accept()` — pre-existing паттерн, тот же в `/kanban`+`/shifts`). QA: 3/5 AC подтверждены полностью (AC2/AC3/AC5), AC1 parity + AC4 real-time — частично. Тесты: 28 ARCH-014 + 530 api/services passed, 0 регрессий. **FIX-008 вынесен в отдельный ticket** (D-FIX008, out of scope). Follow-up gaps (не блокеры): yard/apartment emit без тестов, WS e2e непокрыт, `AddressValidationError` — мёртвый код. План: `~/.claude/plans/dynamic-jumping-pumpkin.md`.

#### ARCH-015 — `pair_with_next` плоская модель
- **Priority:** P1 — **Type:** ARCH / UX — **Source:** A (M1), Q (BUG-4)
- **Files:** `api/board_config/schemas.py`, `frontend/src/types/boardConfig.ts`, `frontend/src/pages/BoardEditorPage.tsx`, `ResidentBoardPage.tsx`
- **Description:** Флаг "склей со следующим" не масштабируется на 3+ модуля в ряд. Compensating logic в editor (сброс tail-флага, disable при scoped визибилити).
- **Fix:** Заменить `LayoutItem[]` на `{ rows: { id, modules: ModuleId[] }[] }` + миграция данных.
- **AC:** В editor можно создать 3-в-ряд; backward-compat: старые конфиги (без rows) парсятся как flat → 1 модуль на row.
- **Estimate:** 1 d.

#### ARCH-016 — Один EventBus вместо двух путей emit
- **Priority:** P1 — **Type:** ARCH — **Source:** A (C2, R2)
- **Files:** новый `services/event_bus.py` + рефакторинг address_service/router
- **Description:** `building.created` из bot → только webhook, из API router → webhook + Redis pub/sub. Dashboard не получает WS-обновление при создании из бота.
- **Fix:** Единый EventBus.publish(event, entity_id, payload) → внутри fan-out: `enqueue_webhook` + `publish_redis`.
- **AC:** Создание здания через бот тоже триггерит WS-уведомление на dashboard.
- **Estimate:** 4 h.
- **Status:** ✅ **closed — merged-into ARCH-014** (2026-05-22, PR #23 `732e365`). Реализовано как `services/addresses/events.py` со split-API: `enqueue_outbox` (pre-commit, та же tx) + `publish_realtime_after_commit` (post-commit, best-effort Redis). Bot create/update теперь публикует в Redis наравне с API.

#### ARCH-106 — Миграция секретов в secret-manager (выйти из `.env`-anti-pattern)
- **Priority:** P1 — **Type:** ARCH / SEC — **Source:** Arch review FIX-002 (2026-05-21)
- **Files:** docker-compose.yml, settings.py, .env.example, CI/CD config (если появится), новый `docs/runbooks/secret-management.md`
- **Description:** Backlog FIX-002 решает текущий incident (rotate секретов), но не устраняет корневую проблему — секреты в `.env` на диске. Senior-arch review: end-state должен быть Doppler / 1Password Connect / AWS-SM (Vault overkill для single-dev footprint). Per-env isolation, audit trail, revocable access, CLI injection (`doppler run -- docker compose up`).
- **Fix:** Выбрать платформу (Doppler/1Password/AWS-SM по cost/effort) → bootstrap project там → перенести 8 секретов туда → переписать settings.py чтобы читать через secret-manager SDK или env-inject → удалить `.env` файлы из dev/staging/prod (оставить `.env.example` с пустыми placeholder'ами).
- **AC:** `.env` файлы не содержат значений секретов; settings.py не имеет hardcoded defaults для секретов; rotation одного секрета — single command в secret-manager UI/CLI без правки `.env`; audit-log показывает кто/когда менял.
- **Estimate:** 1 d (Doppler — самый быстрый bootstrap) или 2 d (1Password Connect / AWS-SM).

### Security

#### SEC-017 — TWA refresh token в localStorage
- **Priority:** P1 — **Type:** SEC — **Source:** S (H-1)
- **Files:** `frontend/src/twa/twaClient.ts`, `frontend/src/twa/hooks/useTWAAuth.ts`
- **Description:** 30-дневный TTL refresh-token в localStorage → XSS exfiltration.
- **Fix:** В memory (module variable) + re-issue из `initData` при reopen. Если cross-session нужен — `sessionStorage`.
- **AC:** `localStorage.getItem('twa_refresh_token')` → null после reload.
- **Estimate:** 2 h.

#### SEC-018 — Zustand `auth-store` persistит user.id/roles в localStorage
- **Priority:** P1 — **Type:** SEC — **Source:** S (H-2)
- **Files:** `frontend/src/stores/authStore.ts:41-47`
- **Description:** XSS → читает `localStorage['auth-store']` → user.id + roles.
- **Fix:** `sessionStorage` или отключить persist.
- **AC:** Аналогично SEC-017.
- **Estimate:** 30 min.

#### SEC-019 — `POST /api/v2/auth/set-password` без rate-limit
- **Priority:** P1 — **Type:** SEC — **Source:** S (H-3)
- **Files:** `uk_management_bot/api/auth/router.py:317-328`
- **Description:** Аутентифицированный attacker может brute-force bcrypt в тугом цикле.
- **Fix:** `@limiter.limit("5/minute")`.
- **AC:** 6-й запрос за минуту → 429.
- **Estimate:** 10 min.

#### SEC-020 — `validate_invite_token` не consum'ит nonce → TOCTOU race
- **Priority:** P1 — **Type:** SEC — **Source:** S (H-4)
- **Files:** `uk_management_bot/services/invite_service.py:116`
- **Description:** GET `/api/validate/{token}` валидирует без mark_used → утечка info о валидном токене + race с register.
- **Fix:** **Не consum'ить nonce на GET** (форма регистрации может фетчить validate несколько раз — preview, refresh, ре-валидация после ввода имени). Решение: ввести short-lived (≤60s) replay-cache в Redis с rate-limit 20/мин/IP (см. SEC-096) + structural validation (signature, expiry) на GET. Consume nonce — только на POST `/register` (как сейчас). Чтобы закрыть TOCTOU между двумя одновременными POST — добавить advisory-lock по token-hash на стороне POST, не GET.
- **AC:** (a) GET validate валидного токена дважды подряд → оба раза 200 (refresh формы работает). (b) Два одновременных POST `/register` с одним и тем же токеном → первый успех, второй 409/410 (advisory-lock на token-hash). (c) GET validate на rate-limit'е (>20/мин с одного IP) → 429. (d) Юнит-тест на race: 100 параллельных POST одного токена → ровно один success в БД.
- **Estimate:** 1 h.

#### SEC-021 — `POST /media/upload` без валидации `request_number`/`category`
- **Priority:** P1 — **Type:** SEC — **Source:** S (H-5)
- **Files:** `uk_management_bot/api/main.py:243-272`
- **Description:** Path traversal / injection в downstream media-сервис. GET-эндпоинт валидирует regex, POST нет.
- **Fix:** Применить `REQUEST_NUMBER_PATTERN` regex + whitelist `category` через `FileCategories` enum.
- **AC:** Запрос с `request_number="../../etc/passwd"` → 422.
- **Estimate:** 30 min.

#### SEC-022 — `media_service/.env`: `ALLOWED_ORIGINS=*` + `DEBUG=true`
- **Priority:** P1 — **Type:** SEC / OPS — **Source:** S (H-6)
- **Files:** `media_service/.env`, `media_service/app/core/config.py`
- **Description:** Dev-конфиг, но риск утечки в прод. Open docs (DEBUG=true) + CORS=*.
- **Fix:** Жёстко в `media_service/app/core/config.py` валидатор pydantic: при `DEBUG=False` (prod) запрещать `ALLOWED_ORIGINS == "*"` или содержание `"*"` в списке — fail-fast при загрузке settings. В dev `*` разрешён, как сейчас.
- **AC:** Запуск с `DEBUG=False` + `ALLOWED_ORIGINS=*` → `ValidationError`/exit на старте; в dev (`DEBUG=True`) — стартует как раньше.
- **Estimate:** 30 min.

#### SEC-023 — DDL f-string в `add_performance_indices.py`
- **Priority:** P1 — **Type:** SEC / REFACTOR — **Source:** S (H-7)
- **Files:** `uk_management_bot/database/migrations/add_performance_indices.py:35-37, 74`
- **Description:** Сейчас не эксплуатируемо (hardcoded tuple), но pattern опасен.
- **Fix:** Заменить на `sqlalchemy.sql.elements.quoted_name` или полностью hardcoded DDL.
- **AC:** Нет `text(f"...{var}...")` в файле.
- **Estimate:** 30 min.

### Python / Backend

#### BUG-024 — `web/api/invite.py` использует устаревший `user.role`
- **Priority:** P1 — **Type:** BUG — **Source:** P (C5)
- **Files:** `uk_management_bot/web/api/invite.py:107`
- **Description:** `existing_user.role == "executor"` против deprecated поля. Для пользователей с пустым `role` (после миграции) условие всегда False → теряется specialization при re-registration.
- **Fix:** `existing_user.active_role == "executor"` или `"executor" in (existing_user.roles or [])`.
- **AC:** Регистрация executor'а сохраняет specialization.
- **Estimate:** 15 min.

#### REFACTOR-025 — Дублирование `queue_webhook` / `queue_webhook_sync`
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H1)
- **Files:** `services/webhook_sender.py:83-154`
- **Description:** ~30 строк идентичной логики между sync и async вариантами. В исходном коде есть прямой комментарий "Keep this in sync with queue_webhook" — это явный сигнал предстоящего divergence drift.
- **Fix:** Извлечь `_build_outbox_record()` private helper.
- **AC:** Любая правка payload меняет одно место.
- **Estimate:** 1 h.

#### REFACTOR-026 — `address_service.py` 1413 строк
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H2)
- **Files:** `services/address_service.py`
- **Description:** 7 concerns в одном файле (yards/buildings/apartments/residents/moderation/stats/stepwise-selection).
- **Fix:** Разбить на `yard_service.py`, `building_service.py`, `apartment_service.py`, `resident_service.py`.
- **AC:** Каждый файл ≤ 600 строк.
- **Estimate:** 4 h.

#### REFACTOR-027 — `addresses/router.py` 1207 строк
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H2)
- **Files:** `api/addresses/router.py`
- **Description:** То же, на уровне HTTP-слоя.
- **Fix:** Зеркальное разделение по сущностям.
- **AC:** Каждый файл ≤ 500 строк.
- **Estimate:** 3 h.

#### BUG-028 — `except Exception` 24× в address_service
- **Priority:** P1 — **Type:** BUG / SEC — **Source:** P (H3)
- **Files:** `services/address_service.py`
- **Description:** Глотает всё (IntegrityError, OperationalError, KeyboardInterrupt) и возвращает `f"...: {str(e)}"` в UI — leak схемы.
- **Fix:** Concrete exceptions (`IntegrityError`, `OperationalError` — отдельно); `logger.exception()` для трейса; opaque user-facing messages вида "Не удалось сохранить, попробуйте позже".
- **AC:** Конкретное исключение (например, `UniqueViolation`) → структурированный лог с traceback + generic-сообщение в response. `grep -rn "except Exception" services/address_service.py` → пусто (или явно типизированные).
- **Estimate:** 4 h (по всем 24 методам).

#### REFACTOR-029 — `db.query()` (SA 1.x) в invite.py
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H4)
- **Files:** `uk_management_bot/web/api/invite.py:84`
- **Description:** Deprecated SQLAlchemy 1.x API.
- **Fix:** `db.execute(select(User).where(...)).scalar_one_or_none()`.
- **AC:** Grep `db.query(` в web/ → пусто.
- **Estimate:** 30 min.

#### REFACTOR-030 — `sys.path.append` в web/
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H5)
- **Files:** `web/main.py:15`, `web/api/invite.py:12`
- **Description:** Хрупкая мутация sys.path на import time.
- **Fix:** Удалить; полагаться на `PYTHONPATH` / installed package.
- **AC:** Grep `sys.path.append` в проекте → пусто (за пределами тестов).
- **Estimate:** 15 min.

#### REFACTOR-031 — Lazy logger imports inside except
- **Priority:** P1 — **Type:** REFACTOR — **Source:** P (H6)
- **Files:** `web/api/invite.py:64, 139`
- **Description:** `import logging` в except — глотает ImportError, неконсистентно с остальным кодом.
- **Fix:** `import logging` + `logger = logging.getLogger(__name__)` в module top.
- **AC:** Все except используют module-level logger.
- **Estimate:** 10 min.

#### REFACTOR-032 — f-string logging + PII в address_service
- **Priority:** P1 — **Type:** REFACTOR / SEC — **Source:** P (H7)
- **Files:** `services/address_service.py` ~30 occurrences
- **Description:** `logger.error(f"...{e}")` — eager eval + PII (telegram_id, имена) в plain log.
- **Fix:** `logger.error("...%s", e)` + `logger.exception()`; PII через `extra=` с hashing.
- **AC:** Юнит-тест на отсутствие PII в captured logs.
- **Estimate:** 2 h.

### Infrastructure / Tests (promoted 2026-05-21 после full code review)

#### OPS-109 — GitHub Actions CI: pytest pipeline в docker compose
- **Priority:** P1 — **Type:** OPS — **Source:** Senior-arch full review FIX-001..006 (2026-05-21)
- **Files:** `.github/workflows/bot-tests.yml` (new)
- **Description:** Сейчас `.github/workflows/` отсутствует — 200 passing тестов верифицируется только на dev-машине. "0 regressions across P0" claim не reproducible. Каждый P1+ fix будет полагаться на "работает у меня". До P1 start — must-have.
- **Fix:** Workflow: `docker compose up -d uk-postgres uk-redis` → bind/cp source в bot-container → `docker exec uk-management-bot pytest uk_management_bot/tests/ --tb=short`. Trigger: PR + push to main. Cache pip + docker layers. Включить и vitest job для frontend параллельно.
- **AC:** Создание PR с заведомо ломающим тест → workflow упадёт red; зелёная ветка → можно merge'ить.
- **Estimate:** 2 h.

#### OPS-110 — `docker-compose.dev.yml`: bind-mount source в bot/api containers
- **Priority:** P1 — **Type:** OPS — **Source:** Senior-arch full review (2026-05-21), обнаружено в FIX-001 worker'е
- **Files:** `docker-compose.dev.yml`, потенциально `docker-compose.override.yml`
- **Description:** `uk-management-bot` container НЕ имеет volumes на source → каждое изменение требует full rebuild перед runtime effect. Тесты сейчас работают через `docker cp` workaround — structurally divergent с CI (которая делает `docker build`). Раскладка не позволяет hot-reload в dev.
- **Fix:** Override-файл: `volumes: ["./uk_management_bot:/app/uk_management_bot", "./alembic:/app/alembic"]`. Применять только для dev (не для prod images). `docker-compose.yml` оставить immutable-style как сейчас.
- **AC:** `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` запускает контейнеры с bind-mount; правка `.py` → видна без rebuild. `docker compose up` (prod-style) — без bind-mount, как сейчас.
- **Estimate:** 30 min.

#### SEC-109 — Pydantic Field validation на `RegistrationData.full_name`
- **Priority:** P1 — **Type:** SEC — **Source:** Security review FIX-001 (2026-05-21)
- **Files:** `uk_management_bot/web/api/invite.py:22-28` (`RegistrationData`)
- **Description:** Сейчас `full_name: str` без `Field(max_length=..., pattern=..., min_length=...)`. Атакующий POST'ом `full_name="A" * 10_000_000` или `full_name="‮ malicious admin payload"` (bidi attack) пишет это в `users.first_name` / `users.last_name`, рендерится в admin-уведомлениях. HTTP path bypassа bot-side handler'а который у себя имеет `len(full_name.split()) < 2` check.
- **Fix:** `full_name: str = Field(..., min_length=2, max_length=200, pattern=r"^[\p{L}\p{N}\s\-'\.]+$")` (или ASCII-only если бизнес ОК с тем). Тот же constraint применить в bot-side `handle_full_name_input`. + pre-check на whitespace-only (закрывает [observation full_name-whitespace-bug](./2026-05-20-backlog.md) — IndexError 500).
- **AC:** POST `/api/register` с full_name >200 chars → 422; с bidi-character → 422; с whitespace-only → 422; с валидным `"Иван Петров"` → 200.
- **Estimate:** 1 h (Pydantic + bot-side validator + 4 теста).

#### DB-111 — Переписать FIX-003 migration на `NOT VALID + VALIDATE` для prod-safety
- **Priority:** P1 — **Type:** DB / OPS — **Source:** DB-architect review FIX-003 (2026-05-21)
- **Files:** `alembic/versions/007_set_null_on_request_apartment_fk.py` (rewrite) или новая чистая миграция
- **Description:** Текущий migration делает `DROP CONSTRAINT` + `CREATE FOREIGN KEY` — оба под `ACCESS EXCLUSIVE` lock; CREATE FK с validation сканирует всю таблицу. На live `requests` (>100k rows) — реальный outage, не "brief downtime". Dev — OK; prod — **blocker**.
- **Fix:** Split в 3 шага: (a) `ADD CONSTRAINT ... ON DELETE SET NULL NOT VALID` (instantaneous, только pg_constraint update); (b) `ALTER TABLE requests VALIDATE CONSTRAINT ...` (`ShareUpdateExclusiveLock`, non-blocking reads); (c) `DROP CONSTRAINT requests_apartment_id_fkey` (instantaneous, теперь старый constraint лишний). Downgrade — обратный порядок с тем же 3-step pattern.
- **AC:** Текущий тест `test_apartment_purge_keeps_request_history` продолжает passить. Locking-наблюдение: на dev снимок `pg_locks` во время migration не показывает `AccessExclusiveLock` дольше 100ms на `requests`.
- **Estimate:** 1 h (rewrite + test + manual `pg_locks` observation).
- **Blocking:** prod-deploy `fix/fix-003-fk-purge-set-null` до выполнения этого item'а.

#### OPS-112 — Prod-верификация webhook-hardening (8 чеков)
- **Priority:** P1 — **Type:** OPS — **Source:** UK↔InfraSafe webhook-hardening spec (2026-05-19), анализ 2026-05-22
- **Files:** — (operational, без правок кода)
- **Description:** Production-incident 2026-05-19 (building id=1 «Olmazor City 14V» не дошёл в InfraSafe, `webhook_outbox` пуст). Fix-комплект **PR-A/B/C/D реализован коммитом `a20686f` (2026-05-19) и в `main`**: SKIP LOCKED race-fix, fail-loud `queue_webhook`, `REDIS_PUBSUB_URL_RESOLVED`, `/api/health/outbox`, cycle-log, `reconcile_buildings`. PR-E (CSP Google Fonts) закрыт ещё `7273d3e` (2026-04-12, Caddyfile). **Однако 8 post-deploy verification-чеков из спеки на проде не прогонялись** — нет доказательства, что инцидент реально устранён в проде. Спека: `Infrasafe/docs/superpowers/specs/2026-05-19-uk-infrasafe-webhook-hardening.md`, секция Verification.
- **Fix:** Прогнать 8 чеков с Mac: (1) settings sanity — `INFRASAFE_WEBHOOK_ENABLED=True`, `REDIS_PUBSUB_URL_RESOLVED` с паролем; (2) `curl .../uk/api/health/outbox` → JSON; (3) `docker logs uk-management-api | grep 'process_outbox cycle'`; (4) e2e — создать здание в Dashboard → ≤15s в InfraSafe БД; (5) race — нет дублей в `integration_log` (`GROUP BY entity_id HAVING COUNT(*)>1` → 0); (6) `grep -i redis.*auth` → пусто; (7) `grep reconcile_buildings` через ~5 мин после старта api; (8) drift-recovery — выключить webhook, создать здание, включить, через ~1ч reconcile найдёт+replay'нёт.
- **AC:** Все 8 чеков пройдены, результат задокументирован в `docs/audit/verifier-logs/OPS-112.md`. При провале любого — заводится bug.
- **Estimate:** 1 h.

### Integration findings (review интеграции UK↔InfraSafe, 2026-05-22)

Найдено при ревизии фактического состояния интеграции (анализ `webhook_sender.py`, `reconciliation.py`, `events.py`, emit-точек). FIX-007 (входящий канал) уже в бэклоге — здесь не дублируется.

#### ARCH-113 — Заявки из бота не эмитят `request.*` webhook → не доходят в InfraSafe
- **Priority:** P1 (candidate P0 — см. note) — **Type:** ARCH / BUG — **Source:** integration review 2026-05-22
- **Files:** `uk_management_bot/services/request_service.py`, `uk_management_bot/handlers/requests.py`; emit-эталон — `uk_management_bot/api/requests/router.py:263-265, 373-375`
- **Description:** `request.created` / `request.status_changed` webhook эмитятся **только** из REST API (`api/requests/router.py`). Бот-путь создания заявок (`request_service.py`, aiogram-handlers) не вызывает `queue_webhook` вообще — `grep queue_webhook` в `handlers/` и `request_service.py` пуст. Жители подают заявки **через бота** — это основной канал, и эти заявки в InfraSafe не попадают. Это та же two-path drift, что ARCH-014 вылечил для зданий, но для заявок хуже: бот-путь не эмитит ничего, а ARCH-014 унифицировал только адреса.
- **Fix:** Унифицировать emit `request.*` по образцу ARCH-014 — единый async-путь создания/смены статуса заявки (core), вызываемый и из API, и из бота (через адаптер). Минимальный вариант — добавить `enqueue_outbox` + `publish_request_event` в bot request-path, согласовав payload с `build_request_payload`.
- **AC:** Создание заявки через бот → строка в `webhook_outbox` с `event="request.created"`; смена статуса через бот → `request.status_changed`. Payload идентичен API-пути (parity-тест).
- **Estimate:** 4-6 h (зависит от выбора: точечный emit vs полная унификация core).
- **Note по приоритету:** P1 по умолчанию. **Поднять до P0**, если InfraSafe должен видеть заявки жителей для прод-готовности интеграции — уточнить бизнес-контракт с InfraSafe-оператором.

#### ARCH-114 — Reconciliation покрывает только здания, нет `reconcile_requests`
- **Priority:** P1 — **Type:** ARCH — **Source:** integration review 2026-05-22
- **Files:** `uk_management_bot/services/reconciliation.py`, `uk_management_bot/clients/infrasafe_client.py`
- **Description:** `reconcile_buildings` — единственный reconcile-цикл. Для заявок safety-net отсутствует. В связке с ARCH-113 это значит: пропавшие бот-заявки не ловятся даже reconciliation'ом — теряются для InfraSafe навсегда. Even после фикса ARCH-113 любой будущий silent-loss заявки (Bug-2-style) останется незамеченным.
- **Fix:** `reconcile_requests()` по образцу `reconcile_buildings`: fetch InfraSafe-inventory заявок (нужен GET-endpoint на стороне InfraSafe — координация), set-diff, auto-replay через `queue_webhook`. Зависит от ARCH-113 (сначала emit, потом сверка).
- **AC:** Юнит-тест: заявка есть в UK, нет в InfraSafe → `reconcile_requests` enqueue'ит её. Advisory-lock концепция как у buildings.
- **Estimate:** 3 h (+ зависимость от InfraSafe-side endpoint).
- **Blocking:** ARCH-113 (нет смысла сверять то, что не эмитится).

#### SEC-115 — `infrasafe_client` GET `/api/buildings-metrics` без аутентификации
- **Priority:** P3 — **Type:** SEC — **Source:** integration review 2026-05-22
- **Files:** `uk_management_bot/clients/infrasafe_client.py:20-23`
- **Description:** `fetch_infrasafe_external_buildings` делает GET на InfraSafe `/api/buildings-metrics` без auth-заголовка — полагается на изоляцию внутренней docker-сети (`uk-network`). Если сеть не изолирована (мисконфиг compose, shared host) — инвентарь зданий читается кем угодно. Низкий риск, но pattern хрупкий.
- **Fix:** Добавить shared-secret/HMAC или service-token на InfraSafe-side GET-эндпоинты; UK-клиент шлёт заголовок. Координация с InfraSafe.
- **Estimate:** 1 h (UK-side) + InfraSafe-side.

#### TEST-116 — `test_all_valid_urgencies` падает на невалидной `category='Тест'` — ✅ closed
- **Priority:** P3 — **Type:** TEST — **Source:** FIX-007 Phase 2 test run 2026-05-22
- **Files:** `tests/api/test_request_schemas.py:~132` (`TestCreateRequestBody::test_all_valid_urgencies`)
- **Description:** Параметризованный тест по urgency-значениям строил `CreateRequestBody(category='Тест', urgency=X, ...)`. Валидатор `CreateRequestBody.category` отклонял `'Тест'` → параметры падали с `ValidationError` на поле `category`. Не регрессия.
- **Fix:** ✅ Закоммичено (commit `ae0e425`, merged PR #24) — `category='Тест'` → `'Электрика'`. Тест 37/37 зелёный.

#### OPS-117 — Прод-`Dockerfile` не ставит `requirements-dev.txt` — тесты бота не запускаются после пересборки
- **Priority:** P3 — **Type:** OPS — **Source:** FIX-007 build verification 2026-05-22
- **Files:** `Dockerfile:31-33`, `requirements-dev.txt:13` (`aiosqlite==0.20.0`)
- **Description:** Сервис `app` (бот) собирается из прод-`Dockerfile`, который ставит только `requirements.txt` + `uk_management_bot/requirements.txt`. `aiosqlite` (нужен `tests/api/conftest.py` — in-memory `sqlite+aiosqlite://`) лежит в `requirements-dev.txt` и в образ не попадает. CLAUDE.md требует `docker exec uk-management-bot pytest`, но после каждой `docker compose build app` тесты падают с `ModuleNotFoundError: aiosqlite` до ручного `pip install`. Сейчас обойдено ручной установкой в контейнер.
- **Fix:** Либо отдельный test-stage в `Dockerfile` / использовать `Dockerfile.dev` для dev-окружения с `requirements-dev.txt`, либо документировать ручной шаг. Не ставить dev-deps в прод-образ.
- **Estimate:** 1 h.

#### FE-119 — Отображение optional полей InfraSafe `alert.created` в UI заявки
- **Priority:** P3 — **Type:** FE / UX — **Source:** FIX-007 cutover negotiation 2026-05-23 (запрос InfraSafe-стороны)
- **Files:** `frontend/src/pages/RequestDetailsPage.tsx` (или эквивалент), backend: либо JOIN `webhook_inbox` по `request_number`, либо вынести подмножество полей колонками в `requests`
- **Description:** При создании заявки из inbound InfraSafe `alert.created` (FIX-007 Phase 2) optional-поля payload'а сохраняются в `webhook_inbox.payload` JSON, но в UI заявки оператору УК не выводятся. InfraSafe-сторона запросила ticket на отображение для осмысленной операторской работы. Поля и приоритет (по их оценке):
  - **high** — `metric_id` + `metric_value` + `metric_unit` → строка вида «напряжение 215.3 В (норма 220-240 В)»: оператор сразу видит причину
  - **medium** — `infrastructure_type` + `infrastructure_id` → «трансформатор #10» вместо абстрактного «проблема в здании»
  - **low** — `alert_id` (InfraSafe int), `created_at` (alert-side), `correlation_id` (сквозная трассировка через несколько систем)
- **Fix:** Минимальный scope — `metric_*` + `infrastructure_*` в UI. Бэкенд: GET `/api/v2/requests/{number}` обогащает ответ полями из `webhook_inbox.payload->alert` если запись с этим `request_number` существует. Фронт: показывает блок «Источник: InfraSafe alert» если `source_event_id` присутствует.
- **AC:** Для заявки, созданной из `alert.created`, в UI видны метрика+единица+нормальный диапазон (если известен) и идентификатор инфраструктурной единицы. Не блокер cutover'а FIX-007.
- **Estimate:** 3-4 h (backend enrichment + frontend display).

---

## P2 — Плановое (64 total — 16 closed, 48 open)

**Status:** 16 closed (BUG-BOT-007..014, 020..023, 025, 028, 031, 032 — все merged в main через P2/P3/late-discovery batches). 3 newly filed (BUG-BOT-037 prefix-match anti-patterns edit/delete/approve; BUG-BOT-038 status_transitions key mismatch ru/uz). Ниже — initial set + дополнения; полный список ID-helps использовать Closed Items Registry в начале.

Note: эта секция содержит initial-набор P2-задач (~28 на момент создания). Часть пунктов в неё были изначально занесены как P3 (FE-048, DB-056..059, SEC-065..067), но переставлять их между секциями я не стал, чтобы не ломать ссылки и историю — `Priority:` поле в каждом item-е остаётся источником истины. Полный счёт P2 (включая дополнения SEC-083..086, SEC-092, BUG-090, BUG-097, DOCS-095, INV-098, OPS-105) — 43 пункта.

### Frontend

#### FE-033 — `useMemo` без `t` в deps
- **Priority:** P2 — **Type:** BUG — **Source:** T (H1)
- **Files:** `frontend/src/pages/AddressesPage.tsx:233`
- **Description:** При смене языка UI остаётся со старыми кнопками до следующего изменения `searchQuery`/`level`.
- **Fix:** Добавить `t` в deps.
- **Estimate:** 5 min.

#### FE-034 — setState в useEffect body
- **Priority:** P2 — **Type:** BUG — **Source:** T (H2)
- **Files:** `frontend/src/pages/BoardEditorPage.tsx:157-159`
- **Description:** Cascading re-render anti-pattern.
- **Fix:** Lazy initializer или ref-флаг.
- **Estimate:** 15 min.

#### FE-035 — `Date.now()` в render
- **Priority:** P2 — **Type:** REFACTOR — **Source:** T (H3)
- **Files:** `frontend/src/pages/ResidentBoardPage.tsx:85-90`
- **Description:** Impure call внутри render-функции; React Compiler флагает.
- **Fix:** Передавать `now` (уже trackedstate) в `elapsed(iso, now)`.
- **Estimate:** 10 min.

#### FE-036 — Empty `catch {}` без аргумента
- **Priority:** P2 — **Type:** REFACTOR — **Source:** T (H4)
- **Files:** `frontend/src/pages/AddressesPage.tsx:134`
- **Fix:** `catch (_e) { /* storage unavailable */ }`.
- **Estimate:** 1 min.

#### FE-037 — `key={j}` в request card list
- **Priority:** P2 — **Type:** BUG — **Source:** T (M1)
- **Files:** `frontend/src/pages/ResidentBoardPage.tsx:167-170`
- **Fix:** `key={req.id ?? \`${req.number}-${req.status}\`}`.
- **Estimate:** 5 min.

#### FE-038 — confirmDelete dialog: title == description
- **Priority:** P2 — **Type:** BUG / UX — **Source:** T (M2), Q (BUG-5)
- **Files:** `frontend/src/pages/AddressesPage.tsx:521-525, 610-614, 703-707`; `frontend/src/components/addresses/AddressTable.tsx:165-166, 277-278, 389-390`
- **Description:** Заголовок и описание — одна и та же строка с интерполяцией. confirmPurge сделано корректно — асимметрия.
- **Fix:** Добавить `deleteYardTitle` / `deleteYardDesc` ключи в ru/uz и подставить раздельно.
- **Estimate:** 30 min.

#### FE-039 — `patch` helper внутри render scope
- **Priority:** P2 — **Type:** REFACTOR — **Source:** T (M3)
- **Files:** `frontend/src/pages/BoardEditorPage.tsx:171-177`
- **Fix:** `useCallback(...)` или вынести.
- **Estimate:** 10 min.

#### FE-040 — Inline `<style>` с `@import` на каждый ререндер
- **Priority:** P2 — **Type:** PERF — **Source:** T (M5), Q (UX-1)
- **Files:** `frontend/src/pages/ResidentBoardPage.tsx:276`
- **Fix:** Перенести `@import url(...)` в `index.html` или global CSS.
- **Estimate:** 15 min.

#### FE-041 — `console.error` в production
- **Priority:** P2 — **Type:** REFACTOR — **Source:** T (M6)
- **Files:** `frontend/src/hooks/useAddresses.ts` (все 15 onError)
- **Fix:** Structured logger или silent + Sentry capture.
- **Estimate:** 30 min.

#### FE-042 — Bundle 695 kB main + 389 kB AnalyticsPage
- **Priority:** P2 — **Type:** PERF — **Source:** T (Bundle warning)
- **Files:** `frontend/vite.config.ts`
- **Fix:** `React.lazy(() => import('./pages/AnalyticsPage'))`; `manualChunks` для react-dom/dnd-kit/tanstack-query.
- **AC:** Main chunk < 400 kB.
- **Estimate:** 1 h.

#### FE-043 — BoardEditor: нет unsaved-changes guard
- **Priority:** P2 — **Type:** UX — **Source:** Q (BUG-3)
- **Files:** `frontend/src/pages/BoardEditorPage.tsx`
- **Fix:** isDirty + beforeunload + ConfirmDialog при навигации.
- **Estimate:** 1 h.

#### FE-044 — `pair_with_next` без warning для широких модулей
- **Priority:** P2 — **Type:** UX — **Source:** Q (BUG-4)
- **Files:** `frontend/src/pages/BoardEditorPage.tsx`
- **Fix:** Иконка-warning при попытке парировать stats+requests; tooltip "may compress".
- **Estimate:** 30 min.

#### FE-045 — Kanban: нет empty-state в колонках
- **Priority:** P2 — **Type:** UX — **Source:** Q (BUG-6)
- **Files:** `frontend/src/components/kanban/KanbanColumn.tsx:142-169`
- **Fix:** `<EmptyHint>` при `column.requests.length === 0 && !isDragging`.
- **Estimate:** 20 min.

#### FE-046 — Нет offline-indicator в main dashboard
- **Priority:** P2 — **Type:** UX — **Source:** Q (BUG-7)
- **Files:** перенести `frontend/src/twa/components/OfflineIndicator.tsx` → `components/shared/`
- **Fix:** Использовать в `App.tsx`.
- **Estimate:** 30 min.

#### FE-047 — LoginPage 401 interceptor стирает inline-error
- **Priority:** P2 — **Type:** BUG — **Source:** E
- **Files:** `frontend/src/pages/LoginPage.tsx`
- **Description:** `apiClient` пытается рефрешнуть токен на странице логина → редирект перетирает ошибку.
- **Fix:** Использовать `publicClient` (без 401-interceptor) для `/api/v2/auth/login`.
- **Estimate:** 30 min.

#### FE-048 — `STATS` array re-built each render
- **Priority:** P3 — **Type:** PERF — **Source:** T (M4)
- **Files:** `frontend/src/pages/AddressesPage.tsx:241`
- **Fix:** `useMemo` для массива.
- **Estimate:** 10 min.

### Database

#### DB-112 — `requests.address_snapshot` для preservation post-FIX-003 SET NULL
- **Priority:** P2 — **Type:** DB / DATA — **Source:** DB-architect review FIX-003 (2026-05-21)
- **Files:** alembic-миграция; `services/request_service.py` (insert path); `database/models/request.py`
- **Description:** FIX-003 устанавливает `requests.apartment_id` → NULL при apartment purge. **Address context теряется** если `requests.address` text-column не был populated при создании (новый apartment_id-based path может не писать туда). После purge — orphan row без way to reconstruct WHERE заявка была.
- **Fix:** (a) Pre-check: `SELECT COUNT(*) FROM requests WHERE apartment_id IS NOT NULL AND (address IS NULL OR address = '')` — если non-zero, backfill через JOIN. (b) Trigger или application-layer: при INSERT в `requests` с `apartment_id` — снапшотить `apartment.full_address` в `requests.address` (или в новый `address_snapshot` column если нужна явная семантика "snapshot at create time"). (c) Опционально: рассмотреть отдельную `requests_address_history` таблицу для full audit-trail.
- **AC:** После `DELETE apartments WHERE id=X` все `requests` с этим apartment_id имеют непустой `address` (или `address_snapshot`). Существующие orphan-rows backfill'нуты.
- **Estimate:** 2 h (миграция + backfill + trigger/code).
- **Depends on:** FIX-003 merged.

#### DB-049 — `users.roles` хранится как TEXT, не jsonb
- **Priority:** P2 — **Type:** DB — **Source:** D (H1)
- **Files:** alembic-миграция
- **Fix:** `ALTER TABLE users ALTER COLUMN roles TYPE jsonb USING roles::jsonb;` + GIN-индекс.
- **Estimate:** 1 h.

#### DB-050 — requests без индексов на status/user_id/executor_id/created_at
- **Priority:** P2 — **Type:** DB / PERF — **Source:** D (H2)
- **Files:** alembic-миграция
- **Fix:** Composite + single indexes (см. отчёт database-reviewer).
- **Estimate:** 1 h.

#### DB-051 — notifications без индексов на user_id/request_number_fk
- **Priority:** P2 — **Type:** DB / PERF — **Source:** D (H3)
- **Files:** alembic-миграция
- **Fix:** Partial index `WHERE is_read = false` для hot path.
- **Estimate:** 30 min.

#### DB-052 — 37 FK-столбцов без индексов на дочерней стороне
- **Priority:** P2 — **Type:** DB / PERF — **Source:** D (H4)
- **Files:** alembic-миграция
- **Fix:** Полный список FK → index в `database-reviewer` отчёте.
- **Estimate:** 2 h.

#### DB-053 — webhook_outbox без partial index `WHERE status='pending'`
- **Priority:** P2 — **Type:** DB / PERF — **Source:** D (H5)
- **Files:** alembic-миграция
- **Fix:** `CREATE INDEX ix_webhook_outbox_pending ON webhook_outbox(created_at) WHERE status='pending'`.
- **Estimate:** 15 min.

#### DB-054 — board_config.updated_by без FK и без индекса
- **Priority:** P2 — **Type:** DB — **Source:** D (H6)
- **Fix:** Add FK ON DELETE SET NULL + index.
- **Estimate:** 30 min.

#### DB-055 — Все таблицы integer PK
- **Priority:** P2 — **Type:** DB — **Source:** D (M1)
- **Files:** alembic-миграция для append-heavy (audit_logs, requests, notifications, webhook_outbox, shift_assignments)
- **Fix:** bigint PK + bigint FK.
- **Estimate:** 4 h (с FK).

#### DB-056 — board_config.data — json, не jsonb
- **Priority:** P3 — **Type:** DB — **Source:** D (M3)
- **Fix:** `ALTER TABLE ... ALTER COLUMN data TYPE jsonb`.
- **Estimate:** 15 min.

#### DB-057 — webhook_outbox.status без CHECK constraint
- **Priority:** P3 — **Type:** DB — **Source:** D (M4)
- **Fix:** `ADD CONSTRAINT ... CHECK (status IN ('pending','sent','failed'))`.
- **Estimate:** 10 min.

#### DB-058 — Дубликаты btree-индексов на PK
- **Priority:** P3 — **Type:** DB — **Source:** D (Redundant indexes)
- **Files:** 22 индексов вида `ix_<table>_id`
- **Fix:** Drop; в моделях `index=False` на PK столбцах.
- **Estimate:** 30 min.

#### DB-059 — Миграция 006 импортирует app code на module-level
- **Priority:** P3 — **Type:** DB — **Source:** D
- **Files:** `alembic/versions/006_*.py`
- **Description:** `from uk_management_bot... import DEFAULT_BOARD_CONFIG` на module level → ModuleNotFoundError если запускать вне installed package.
- **Fix:** Перенести import в `upgrade()` или inline-ить default.
- **Estimate:** 15 min.

#### DB-060 — `users.role` legacy column рядом с `roles`
- **Priority:** P2 — **Type:** DB / REFACTOR — **Source:** D (M2)
- **Files:** миграция на drop column
- **Fix:** Полный cut-over: убрать `role` после verify что `active_role` + `roles` достаточно.
- **Estimate:** 1 d (включая аудит call-sites).

### Security / Hardening

#### SEC-110 — Rate-limit на `/api/register` keyed на telegram_id (secondary bucket beyond IP)
- **Priority:** P2 — **Type:** SEC — **Source:** Security review FIX-001 (2026-05-21)
- **Files:** `uk_management_bot/web/api/invite.py:72` (decorator), `uk_management_bot/web/limiter.py`
- **Description:** Сейчас `@web_limiter.limit("3/minute")` на `/api/register` keyed на IP (через `X-Real-IP`). IPv6 rotation / cloud-VPN ranges тривиально bypass'ят, а shared NAT может ложно блокировать. Secondary bucket по `telegram_id` (или `data.token[:8]`) ловит attack-pattern "много токенов на одного user'а".
- **Fix:** Custom key function в slowapi: composite `f"{client_ip}:{data.telegram_id}"` — каждый bucket независим, оба должны быть в limit. Альтернативно: 2 separate limit'а через `@web_limiter.limit("3/minute", key_func=...)`.
- **AC:** Юнит-тест: 4 запроса с одного IP но разными telegram_id за минуту — НЕ блокируются по IP-only; 4 запроса с одного telegram_id с разных IP — блокируются на 4-м по telegram_id-bucket.
- **Estimate:** 1 h.

#### SEC-061 — Нет security headers на api/main.py
- **Priority:** P2 — **Type:** SEC — **Source:** S (M-1)
- **Fix:** Middleware с `X-Content-Type-Options`, `X-Frame-Options`, `HSTS`, `Referrer-Policy`.
- **Estimate:** 30 min.

#### SEC-062 — Rate-limiter swallow_errors=True (fail-open)
- **Priority:** P2 — **Type:** SEC — **Source:** S (M-2)
- **Files:** `api/rate_limit.py:37-39`
- **Fix:** Alerting на Redis unreachability; partial fail (allow + log).
- **Estimate:** 1 h.

#### SEC-063 — `INFRASAFE_WEBHOOK_URL` без allowlist
- **Priority:** P2 — **Type:** SEC — **Source:** S (M-3)
- **Fix:** При startup verify scheme + hostname против allowlist.
- **Estimate:** 30 min.

#### SEC-064 — `outbox_health` без auth
- **Priority:** P2 — **Type:** SEC — **Source:** S (M-4)
- **Fix:** Bearer/IP-allowlist в nginx или dependency.
- **Estimate:** 15 min.

#### SEC-065 — Hardcoded Telegram channel IDs в media_service config
- **Priority:** P3 — **Type:** SEC — **Source:** S (M-5)
- **Fix:** Удалить defaults; обязательная env-конфигурация.
- **Estimate:** 15 min.

#### SEC-066 — Слабый default `secret_key` в media_service
- **Priority:** P3 — **Type:** SEC — **Source:** S (M-6)
- **Fix:** Default → пустая строка + fail-fast при load если empty.
- **Estimate:** 15 min.

#### SEC-067 — `validate_invite_token` leak'ит internal error message
- **Priority:** P3 — **Type:** SEC — **Source:** S (M-8)
- **Files:** `services/invite_service.py:130`
- **Fix:** Generic `"invalid token"` вместо `str(e)`.
- **Estimate:** 10 min.

### Tests / QA

#### TEST-068 — Frontend vitest: 0 тестовых файлов
- **Priority:** P2 — **Type:** TEST — **Source:** vitest run
- **Files:** новые тесты
- **Description:** Покрытие 0%. Глобальное правило 80%.
- **Fix:** Стартовый набор:
  - `pair_with_next` default/toggle/reorder reset
  - `useAddresses` mutations
  - `confirmDelete` vs `confirmPurge` state
  - Auth store roles parsing
- **Estimate:** 1 d (initial sprint).

#### TEST-069 — Backend pytest: 150 collection errors из legacy import
- **Priority:** **P1** (promoted 2026-05-21 from P2 per senior-arch full code review) — **Type:** TEST — **Source:** pytest
- **Files:** `tests/test_data_export.py`, `tests/test_google_sheets_integration.py`, `tests/test_invite_integration.py`, `tests/test_onboarding.py`, `tests/test_performance.py`, `tests/test_purchase_fix.py`, `tests/test_request_service.py`, `tests/test_shift_service.py`, `tests/test_simple_sheets_sync.py`, `tests/test_auth_service_role_switch.py`
- **Description:** Старые тесты используют импорты `services.x` / `database.connection` / etc. без `uk_management_bot.` префикса; триггерят `Table 'users' is already defined` mapper-конфликт. (Полный список — 10 файлов; это шире, чем 4 файла в TL;DR `full-review.md` — там перечислены только самые "горячие".) **Promotion rationale (2026-05-21):** до пока эти 10 файлов skipped, coverage в P1 неинтерпретируема — нельзя верить cumulative test count'у. Senior-arch рекомендует **go-first** перед G1-группой P1 (EventBus + AddressService unification, 9 файлов overlap).
- **Fix:** Привести импорты к `uk_management_bot.*`; добавить `--ignore` в pytest.ini для несовместимых.
- **Estimate:** 2 h.
- **Block:** не блокирует P1 prima facie, но senior-arch рекомендация — выполнить раньше других P1 items, чтобы тесты в P1 имели валидный baseline.

#### TEST-070 — Покрытие pair_with_next тестами
- **Priority:** P2 — **Type:** TEST — **Source:** Q (UX-4)
- **Files:** `tests/api/test_board_config.py` (новый), `frontend/src/pages/__tests__/BoardEditorPage.test.tsx` (новый)
- **AC:** Pydantic accepts old/new layout; Editor toggle/reorder/visibility работают.
- **Estimate:** 2 h.

#### TEST-071 — E2E fixture для login flow
- **Priority:** P2 — **Type:** TEST / OPS — **Source:** E
- **Files:** `scripts/seed_e2e_user.py`, `tests/e2e/.env.example`
- **Description:** `successful login redirects` тест skipped.
- **Fix:** Seed-скрипт + env-переменные.
- **Estimate:** 1 h.

---

## P3 — Nice-to-have / долгосрочное (49 total — 9 closed, 40 open)

**Status:** 9 closed (BUG-BOT-015/016/024/026/027/029/030 — все merged через P3 batch; ARCH-102 — closed merged-into ARCH-014 PR #23 2026-05-22; TEST-116 — fixed commit `ae0e425`, merged PR #24 2026-05-22). BUG-BOT-039/040 + SEC-115 (infrasafe_client no-auth) newly filed 2026-05-22. OPS-117 (Dockerfile без requirements-dev) filed 2026-05-22 при сборке FIX-007. FE-119 (UI optional InfraSafe alert fields) filed 2026-05-23 при cutover-переговорах. Открытые включают BUG-BOT-017 (junk data manual cleanup), BUG-BOT-034 (accept_/purchase_ prefix anti-patterns), BUG-BOT-035 (cancel_clarification edge-case).

Note: initial-набор P3 (~11 пунктов NICE-072..082). Дополнительные P3 (REFACTOR-088, REFACTOR-089, REFACTOR-091, INV-087, PERF-093, FE-099, FE-100, ARCH-102, DB-103, DB-104) физически лежат в секции "Дополнения" ниже.

#### NICE-072 — Сгенерировать TS-типы из Pydantic
- **Priority:** P3 — **Type:** REFACTOR — **Source:** A (M2)
- **Description:** ModuleId dублирован Python↔TS; через `pydantic2ts` / OpenAPI export.
- **Estimate:** 4 h.

#### NICE-073 — Один источник истины для defaultBoardConfig
- **Priority:** P3 — **Type:** REFACTOR — **Source:** A (M3)
- **Description:** 6 точек синхронизации.
- **Estimate:** включено в NICE-072.

#### NICE-074 — Per-worker board_config cache → Redis
- **Priority:** P3 — **Type:** PERF — **Source:** A (M4)
- **Files:** `api/public/router.py`
- **Fix:** Redis-cache с TTL.
- **Estimate:** 2 h.

#### NICE-075 — Public board: N+1 на PIPELINE_STATUSES
- **Priority:** P3 — **Type:** PERF — **Source:** A (M5)
- **Fix:** Один SELECT с conditional aggregates / window function.
- **Estimate:** 1 h.

#### NICE-076 — purge_* race с FOR UPDATE
- **Priority:** P3 — **Type:** ARCH — **Source:** A (M6)
- **Description:** Между GET children COUNT и DELETE возможна гонка.
- **Fix:** `SELECT ... FOR UPDATE` на родителя или SERIALIZABLE.
- **Estimate:** 1 h.

#### NICE-077 — Общая `auth_dependency` для bot/api/web
- **Priority:** P3 — **Type:** ARCH — **Source:** A (M7)
- **Estimate:** 2 h.

#### NICE-078 — `_parse_user_roles` дублирован
- **Priority:** P3 — **Type:** REFACTOR — **Source:** A (M8)
- **Fix:** `User.roles_list` @property.
- **Estimate:** 1 h.

#### NICE-079 — Backend-валидация `pair_with_next` на последнем элементе
- **Priority:** P3 — **Type:** BUG — **Source:** A (M9)
- **Description:** Прямой PUT без UI guard сохраняет невалидный конфиг.
- **Fix:** Pydantic validator: `pair_with_next=True` на последнем видимом → error.
- **Estimate:** 30 min.

#### NICE-080 — Sentry для web/main.py [merged → ARCH-009]
- **Priority:** P3 — **Type:** OPS — **Source:** A (H1)
- **Status:** merged-into ARCH-009 (sub-task)
- **Files:** `uk_management_bot/web/main.py`
- **Fix:** `sentry_sdk.init(...)` в web bootstrap, как в api.
- **Estimate:** 30 min (включено в ARCH-009).

#### NICE-081 — DELETE `/purge` без audit log
- **Priority:** P3 — **Type:** SEC — **Source:** S (L-2)
- **Fix:** Audit-запись перед `db.delete()`.
- **Estimate:** 30 min.

#### NICE-082 — Refresh-token TTL 30d, нет sliding window
- **Priority:** P3 — **Type:** SEC — **Source:** S (L-4)
- **Fix:** Сократить до 7d или sliding renewal.
- **Estimate:** 1 h.

---

## Что не покрыто бэклогом

- Долгосрочная архитектурная декомпозиция (`shared/` пакет, разнесение bot/api/web — см. отчёт architect, R1) — это эпик, не один Issue.
- Полный rewrite address_service на async (FIX-008 решение 2) — отдельный эпик.
- Observability: Prometheus/Grafana, distributed tracing, request-id propagation — отдельный эпик.

---

## Дополнения после self-cross-check (P1/P2/P3 mixed)

Эта секция исторически называлась "P2/P3 дополнения", но фактически содержит и P1 (SEC-096), и P2 (большинство), и P3 (REFACTOR-088, REFACTOR-089, REFACTOR-091, PERF-093, INV-087, FE-099, FE-100, ARCH-102). Сортировка — по моменту обнаружения, не по Priority. При планировании итерации фильтровать по `Priority:` поле, не по принадлежности секции.

### Security / Backend hardening (доп.)

#### SEC-083 — `ADMIN_PASSWORD` URL-encoded short-string обход length check
- **Priority:** P2 — **Type:** SEC — **Source:** S (L-1)
- **Files:** `.env`, `settings.py:169`
- **Description:** Текущее значение в `.env` — `Inf%40%24afe` (12 символов в URL-кодированной форме). Декодируется в `Inf@$afe` — **8 символов**. Текущий guard в settings проверяет длину **до** декодирования и пропускает, потому что 12 ≥ 12. Реальная стойкость пароля — 8 печатных символов.
- **Fix:** Декодировать `urllib.parse.unquote(value)` перед длиной/энтропией проверкой; поднять `MIN_ADMIN_PASSWORD_LENGTH` с 12 до 16; добавить charset-check (не только длина).
- **AC:** Запуск с `ADMIN_PASSWORD=Inf%40%24afe` падает на старте с понятным сообщением; запуск с 16+ декодированных символов проходит.
- **Estimate:** 30 min.

#### SEC-084 — `PUT /api/v2/board-config` без rate-limit
- **Priority:** P2 — **Type:** SEC — **Source:** S (L-3)
- **Files:** `api/board_config/router.py:54`
- **Description:** GET имеет `120/min`, PUT (manager-only) — без лимита.
- **Fix:** `@limiter.limit("30/minute")`.
- **Estimate:** 5 min.

#### SEC-085 — `outbox_health` возвращает `str(e)` на DB-ошибке (может включать connection string)
- **Priority:** P2 — **Type:** SEC — **Source:** S (L-6)
- **Files:** `api/main.py:207`
- **Fix:** Generic `"db_error"`.
- **Estimate:** 5 min.

#### SEC-086 — Verify Jinja2 autoescape для `/register/{token}` template
- **Priority:** P2 — **Type:** SEC — **Source:** S (L-7)
- **Files:** `web/main.py:85`, templates
- **Description:** Сырой токен в context — нужно убедиться, что `autoescape=True` в Jinja2Templates loader.
- **Fix:** Verify + добавить регресс-тест.
- **Estimate:** 30 min.

#### SEC-092 — Debug routes `/test`, `/simple`, `/minimal` активны в проде
- **Priority:** P2 — **Type:** SEC — **Source:** P (M5)
- **Files:** `web/main.py:87-100`
- **Description:** Грузят Telegram WebApp SDK; в проде должны быть отключены.
- **Fix:** Wrap в `if settings.DEBUG:` или удалить.
- **Estimate:** 10 min.

#### SEC-096 — `validate_invite_token` GET без rate-limit (brute force)
- **Priority:** P1 — **Type:** SEC — **Source:** Q (RISK-2)
- **Files:** `web/api/invite.py:37`
- **Fix:** `@web_limiter.limit("20/minute")` per IP.
- **Estimate:** 10 min.

### Python refactoring (доп.)

#### REFACTOR-113 — Lazy logging convention (eager f-string → `%s` per-arg) sweep
- **Priority:** P3 — **Type:** REFACTOR — **Source:** Python-reviewer full code review (2026-05-21); expands REFACTOR-032 scope
- **Files:** project-wide; ≥50 occurrences по `grep -rn "logger\.\(info\|debug\|error\|warning\)(f\"" uk_management_bot/`
- **Description:** Python logging convention рекомендует `logger.info("text %s", var)` вместо `logger.info(f"text {var}")` — string не форматируется если log level disabled. Сейчас тотально `f"..."` (legacy + новые тесты + новые фиксы FIX-001/003/005/006). Также f-string'и с exception'ами могут leak'нуть sensitive context (cross-ref FIX-006/REFACTOR-032).
- **Fix:** Bulk-rewrite через ruff `G004` rule (`logging-f-string`) auto-fix. Применять файл за файлом, валидировать тестами после каждого батча.
- **AC:** `ruff check --select=G004 uk_management_bot/` → 0 violations. Все существующие тесты — green.
- **Estimate:** 2-3 h (несколько проходов по файлам).

#### NICE-114 — Стандартизация test conventions (pytest-asyncio, marks, future-annotations)
- **Priority:** P3 — **Type:** TEST / REFACTOR — **Source:** Python-reviewer full code review (2026-05-21)
- **Files:** `uk_management_bot/tests/*.py`, `pyproject.toml` (или `pytest.ini`), `conftest.py`
- **Description:** 4 новых теста (FIX-001/003/005/006) написаны в 4 разных стилях: `_run(coro)` DIY-asyncio (FIX-001) vs `@pytest.mark.asyncio` (FIX-005/006); `monkeypatch` vs `patch.object` (mix'ed); `from __future__ import annotations` inconsistent; нет `@pytest.mark.unit`/`integration`. Свидетельствует об отсутствии shared test conventions doc.
- **Fix:** (a) Завести `docs/development/testing-conventions.md` с правилами. (b) `pyproject.toml`: `asyncio_mode = "auto"`, `markers = ["unit", "integration", "e2e"]`. (c) Sweep 4 новых файла: убрать `_run` (use mark.asyncio), добавить markers, добавить `from __future__ import annotations` везде. (d) Configure ruff/flake8 для enforcement.
- **AC:** `pytest --collect-only -m unit` собирает unit-tests; `pytest -m integration` — только integration. Все тесты идиоматичны (один стиль).
- **Estimate:** 2 h.

#### REFACTOR-088 — Implicit Optional без аннотации
- **Priority:** P3 — **Type:** REFACTOR — **Source:** P (M1)
- **Files:** `services/address_service.py:1256`
- **Description:** `comment: str = None` — type-hint неверен.
- **Fix:** `comment: Optional[str] = None`.
- **Estimate:** 5 min.

#### REFACTOR-089 — Magic status strings вместо Enum
- **Priority:** P3 — **Type:** REFACTOR — **Source:** P (M2)
- **Files:** `services/address_service.py`, `api/addresses/router.py`
- **Description:** `'pending'/'approved'/'rejected'` × 20+ occurrences.
- **Fix:** `class UserApartmentStatus(str, Enum)`.
- **Estimate:** 30 min.

#### BUG-090 — `search_apartments` без `.limit()` в service
- **Priority:** P2 — **Type:** PERF — **Source:** P (M3)
- **Files:** `services/address_service.py:586`
- **Description:** Router ограничивает 50, service-метод без лимита — unbounded fetch.
- **Fix:** `.limit(100)` или конфиг.
- **Estimate:** 10 min.

#### REFACTOR-091 — Reconciliation advisory lock scope > commit
- **Priority:** P3 — **Type:** REFACTOR — **Source:** P (M4)
- **Files:** `services/reconciliation.py:136, 152`
- **Description:** `db.commit()` внутри `try`, lock освобождается в `finally`. Lock держится дольше, чем нужно.
- **Fix:** Перенести commit или сузить scope.
- **Estimate:** 20 min.

#### PERF-093 — `get_statistics` делает 8 SELECT'ов
- **Priority:** P3 — **Type:** PERF — **Source:** P (M6)
- **Files:** `services/address_service.py:944`
- **Fix:** Один SELECT с `func.filter()` / conditional aggregates.
- **Estimate:** 1 h.

#### INV-087 — hmac.new — investigation
- **Priority:** P3 — **Type:** REFACTOR — **Source:** P (C2)
- **Description:** Python-reviewer сначала пометил critical, в описании сам отозвался ("this call actually works"). Стабильная альтернатива — `hmac.digest(secret, msg, "sha256").hex()`.
- **Fix:** Переход на `hmac.digest()` для стабильности при апгрейдах stdlib.
- **Estimate:** 10 min.

### Frontend UX (доп.)

#### FE-094 — Bot-side ошибки address_service не локализованы
- **Priority:** P2 — **Type:** UX / I18N — **Source:** Q (BUG-2)
- **Files:** `handlers/address_buildings.py:493,497`; `services/address_service.py` все error returns
- **Description:** Бот для UZ-пользователя выводит русские строки (`"Двор не найден"`).
- **Fix:** Service возвращает error code (Enum) → handler мапит на `get_text(code, language)`.
- **Estimate:** 4 h (по всему service).

#### FE-099 — Иконка `Columns2` в editor vs unicode `⛶` в подсказке
- **Priority:** P3 — **Type:** UX — **Source:** Q (UX-2)
- **Files:** `frontend/src/i18n/locales/{ru,uz}.json` (`boardEditor.modulesHint`)
- **Description:** В подсказке упоминается `⛶`, в реальном UI — `Columns2` от lucide-react.
- **Fix:** Заменить unicode на текстовое описание ("иконка с двумя колонками") или вынести SVG в подсказку.
- **Estimate:** 15 min.

#### FE-100 — Stat-tile residents показывает `${approved}+${pending}`
- **Priority:** P3 — **Type:** UX — **Source:** Q (UX-3)
- **Files:** `frontend/src/pages/AddressesPage.tsx`
- **Description:** Несогласованный формат против остальных `${active}/${total}` плиток.
- **Fix:** Либо привести к общему формату, либо добавить tooltip "approved + pending".
- **Estimate:** 30 min.

### Risks → конкретные задачи

#### DOCS-095 — Outbox INSERT failure rollback'ит building create
- **Priority:** P2 — **Type:** DOCS / OPS — **Source:** Q (RISK-1)
- **Description:** Transactional outbox = building.create откатится, если webhook_outbox INSERT упадёт. Это by design, но нужно alerting + документировать.
- **Fix:** Alert на `webhook_outbox INSERT errors > 0/min`; runbook в `docs/runbooks/outbox-failures.md`.
- **Estimate:** 1 h.

#### BUG-097 — `update_building` не сбрасывает GPS на null
- **Priority:** P2 — **Type:** BUG — **Source:** Q (RISK-3)
- **Files:** `services/address_service.py:280-338`
- **Description:** Bot-service использует `if gps_latitude is not None` → нельзя сбросить координаты обратно в null через update; InfraSafe не узнает.
- **Fix:** Различать "не передано" (sentinel) vs "передано как None"; для второго случая — выполнить set + emit webhook.
- **Estimate:** 1 h.

#### INV-098 — `RequestNumberService` overflow на 999/day
- **Priority:** P2 — **Type:** INVESTIGATION — **Source:** Q (RISK-4)
- **Description:** Формат `YYMMDD-NNN` ломается на 1000-й заявке в день. Существующая запись `0076` (4-digit) подозрительна — либо overflow уже был, либо нарушен формат в legacy data.
- **Fix:** Аудит существующих request_number'ов; если 4-digit найдены — root cause analysis + миграция формата.
- **Estimate:** 2 h investigation + неизвестно для fix.

### Architecture (доп.)

#### ARCH-115 — Branch lifecycle policy + stale-branch reaper
- **Priority:** P3 — **Type:** OPS / ARCH — **Source:** Senior-arch full code review (2026-05-21)
- **Files:** new `docs/development/branch-policy.md`; possibly `.github/workflows/stale-branches.yml`
- **Description:** На момент аудита `git branch` показывает ≥15 feature/fix веток, включая `feat/arch-015-rows-wip` 2-недельной давности (preserved per coordinator decision). Нет policy "когда удалять / архивировать stale branches". Каждый новый fix risks конфликт с забытой WIP. Senior-arch предлагает 30-day stale-branch deletion rule.
- **Fix:** (a) Doc `docs/development/branch-policy.md` с правилами naming (`fix/<id>-<slug>`, `feat/<name>`, `hotfix/<urgency>`), retention (30 days post-merge → delete, 90 days unmerged → archive tag), labeling. (b) GitHub Action `stale-branches.yml`: weekly cron, list branches no commits >30d, comment in repo issue. (c) Применить retroactively к текущему set'у.
- **AC:** `git for-each-ref --sort=-committerdate refs/heads/` показывает не больше N веток (где N = active in-flight items + main). Policy документирована и enforced.
- **Estimate:** 2 h policy + 1 h workflow.

#### ARCH-107 — JWT `kid`-based dual-key verifier (graceful JWT_SECRET rotation)
- **Priority:** P2 — **Type:** ARCH / SEC — **Source:** Arch review FIX-002 (2026-05-21)
- **Files:** `uk_management_api/api/auth/jwt_service.py` (или равный), `uk_management_api/config/settings.py`
- **Description:** Сейчас при rotation `JWT_SECRET` все активные web/TWA сессии force-logout одновременно (нет способа верифицировать токены подписанные OLD-ключом). Senior-arch review FIX-002 surfaced: добавить `kid` (key-id) header в JWT + хранить набор {primary, next} ключей с тем же flag-flow что у `INFRASAFE_USE_NEXT_SECRET` → graceful rotation без logout.
- **Fix:** При issue: `jwt.encode(payload, primary_key, algorithm='HS256', headers={'kid': 'primary'})`. При verify: читать `kid` из header, попробовать соответствующий ключ из set; падать только если ни один не подходит. Settings содержит `JWT_SECRET` + опциональный `JWT_SECRET_NEXT` с `JWT_USE_NEXT=false/true`.
- **AC:** Юнит-тест: токен подписанный OLD-ключом продолжает проходить verify после переноса OLD → NEXT и issue новых на NEW. После полного rotation (NEXT → primary, удаление OLD) — OLD токены отклоняются.
- **Estimate:** 4 h.

#### ARCH-108 — Перенос ownership БД на uk_admin, GRANT uk_bot только DML
- **Priority:** P2 — **Type:** SEC / DB — **Source:** DB-arch review FIX-004 (2026-05-21), gating-выход из FIX-004
- **Files:** новая Alembic-миграция + runbook reassign
- **Description:** FIX-004 (NOSUPERUSER) убирает `DROP DATABASE` / `CREATE ROLE` риск, но `uk_bot` остаётся owner всех таблиц/sequences/индексов и может `DROP TABLE` / `TRUNCATE` под любой SQLi. Полное remediation: reassign ownership на `uk_admin`, grant `uk_bot` только DML (SELECT/INSERT/UPDATE/DELETE без REFERENCES/TRIGGER/owner-DDL). Все плановые DDL впредь запускать под `uk_admin` (или новая dedicated `migration_user` роль с CREATE), `uk_bot` — runtime-only.
- **Fix:** (a) `REASSIGN OWNED BY uk_bot TO uk_admin` в чистой миграции; (b) явные `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO uk_bot` + `USAGE, SELECT, UPDATE ON ALL SEQUENCES`; (c) `REVOKE CREATE ON SCHEMA public FROM uk_bot`; (d) `ALTER DEFAULT PRIVILEGES FOR ROLE uk_admin` для grants на новых объектах; (e) обновить alembic-runner чтобы подключался под `uk_admin` (или dedicated migration user), не под `uk_bot`.
- **AC:** `SELECT tableowner FROM pg_tables WHERE schemaname='public';` показывает `uk_admin` для всех таблиц. SQLi-payload вида `'; DROP TABLE requests;--` падает с permission denied. Приложение продолжает работать без regressions.
- **Estimate:** 3-4 h (миграция + runbook + verify).
- **Depends on:** FIX-004 (должен быть `applied` пользователем).

#### ARCH-101 — `web/main.py` не отключает docs/openapi в проде [merged → ARCH-009]
- **Priority:** P2 — **Type:** SEC / ARCH — **Source:** A (H1)
- **Status:** merged-into ARCH-009 (sub-task)
- **Files:** `web/main.py:21`
- **Description:** `FastAPI(title=..., version=...)` без `docs_url=None` в проде → `/docs` открыт.
- **Fix:** `FastAPI(..., docs_url=None if not settings.DEBUG else "/docs", openapi_url=None if not settings.DEBUG else "/openapi.json")`. **Делается в рамках ARCH-009.**
- **Estimate:** 10 min (включено в ARCH-009).

#### ARCH-102 — sync/async engine drift в reconciliation [merged → ARCH-014]
- **Priority:** P3 — **Type:** ARCH — **Source:** A (M10)
- **Status:** ✅ **closed — merged-into ARCH-014** (2026-05-22, PR #23 `732e365`).
- **Description:** Handler пишет через sync, processor читает через async; разные `SessionLocal`/`AsyncSessionLocal` engine'ы.
- **Fix:** Унифицировать через `shared/` пакет (часть R1), либо явно задокументировать инварианты пула.
- **Estimate:** включено в ARCH-014.

---

## Patch после verification-агента (3 items + 1 omitted note)

#### DB-103 — `updated_at` колонки полагаются только на ORM onupdate (нет триггеров)
- **Priority:** P3 — **Type:** DB — **Source:** D (M5)
- **Files:** alembic-миграция; затронуты `users`, `requests`, `notifications`, `apartments`, `buildings`, `yards`, `webhook_outbox`, `board_config`.
- **Description:** Прямые SQL-writes (psql, Alembic data migrations) или сторонние процессы не обновляют `updated_at`. Это привело бы к рассинхрону при любом обходе ORM.
- **Fix:** Создать функцию `set_updated_at()` PL/pgSQL + `BEFORE UPDATE` триггер на каждой таблице.
- **Estimate:** 1 h.

#### DB-104 — `apartments.area` — `double precision` (IEEE 754)
- **Priority:** P3 — **Type:** DB — **Source:** D (M6 bis)
- **Files:** alembic-миграция
- **Description:** Площадь квартиры — потенциально юридическая/финансовая величина; floating-point даёт rounding-error.
- **Fix:** `ALTER TABLE apartments ALTER COLUMN area TYPE numeric(8,2) USING area::numeric(8,2)`.
- **Estimate:** 15 min.

#### OPS-105 — Outbox retention cron + Prometheus метрики (часть architect R3)
- **Priority:** P2 — **Type:** OPS — **Source:** A (R3)
- **Files:** `services/outbox_retention.py` (новый), `api/main.py` lifespan
- **Description:** Outbox растёт линейно: 30+ дней `sent`/`failed` записей не очищаются. Метрики `outbox_pending/failed_24h/oldest_pending` доступны только через HTTP endpoint, не экспортируются в Prometheus.
- **Fix:** Background-task в lifespan: ежесуточно `DELETE ... WHERE status='sent' AND sent_at < now() - interval '30 days'`. Добавить `/metrics` endpoint с prometheus_client.
- **Estimate:** 3 h.

#### NOTE-S-M7 — `AUTH_DATE_MAX_AGE_SECONDS` design tension — **сознательно опущено**
- **Priority:** — — **Type:** — — **Source:** S (M-7)
- **Status:** **omitted (not actionable)**
- **Rationale:** Security-агент описал как «design tension», а не уязвимость: TWA-клиент хранит refresh-token в localStorage и переиспользует его между сессиями, что создаёт давление на расширение 5-минутного freshness check. Решение — SEC-017 (вынести refresh-token из localStorage), после чего этот пункт становится беспредметным.

---

## E2E Button-Graph Testing (2026-05-21) — 17 items

**Источник:** живое тестирование бота через MCP `telegram-qa` под user telegram_id=6055402868 (roles=applicant/executor/manager, status=approved). Полный отчёт + Mermaid-граф + coverage table: `docs/audit/2026-05-21-bot-button-graph.md`. Bot username, обнаруженный в проде: `@Work_space_away_bot` (id=8051385339), не `infrasafebot` как в конфиге.

### Critical / High

#### BUG-BOT-001 — `BOT_USERNAME` mismatch (invite-links ведут на чужого бота)
- **Priority:** P0 — **Type:** CFG / SEC — **Source:** E2E-MCP
- **Files:** `uk_management_bot/config/settings.py:BOT_USERNAME` (default `"infrasafebot"`), `services/invite_service.py`, `api/shifts/router.py`
- **Description:** Реальный username бота, к которому привязан токен — `@Work_space_away_bot` (verified via `getMe`). Но `BOT_USERNAME` default = `"infrasafebot"`, а `.env` его не переопределяет. Invite-link собирается как `https://t.me/{BOT_USERNAME}` → ведёт на **посторонний production бот InfraSafe**, не на наш. Любой инвайт через `📨 Создать приглашение` отправляет пользователя не туда.
- **Fix:** Установить `BOT_USERNAME` в `.env` и `.env.production.template` корректно; убрать дефолт-значение из `settings.py` (или сделать его явно raising при отсутствии env), чтобы misconfig фейлил рано. Добавить startup-check: `getMe()` ↔ `BOT_USERNAME` сравнение, log ERROR при mismatch.
- **AC:** (a) startup-лог содержит assertion `BOT_USERNAME matches getMe().username`; (b) `BOT_USERNAME` нет в settings.py с дефолтом; (c) invite-link smoke-test указывает на правильный бот.
- **Estimate:** 30 min.

#### BUG-BOT-002 — `ShiftTransferService.process_expired_transfers` missing — scheduler ERROR каждые 2ч
- **Priority:** P1 — **Type:** BUG / SCHED — **Source:** E2E-MCP (live logs)
- **Files:** `uk_management_bot/services/shift_transfer_service.py`, `utils/shift_scheduler.py`
- **Description:** `apscheduler` каждые 2 часа вызывает `ShiftTransferService.process_expired_transfers()`. Метод отсутствует → каждый запуск пишет `ERROR Ошибка обработки истекших передач: 'ShiftTransferService' object has no attribute 'process_expired_transfers'`. Просроченные передачи смен не обрабатываются.
- **Fix:** Реализовать метод (стереть запросы передач со статусом `pending` + `expires_at < now()` → перевести в `expired`, уведомить инициатора), либо удалить cron job если функционал не нужен.
- **AC:** Лог не содержит ошибки `process_expired_transfers` после следующего двухчасового запуска; unit-test покрывает истечение pending → expired.
- **Estimate:** 2 h.

#### BUG-BOT-003 — Manager не имеет прав на shift-analytics
- **Priority:** P1 — **Type:** BUG / PERM — **Source:** E2E-MCP
- **Files:** handler/service для `weekly_analytics`, `workload_forecast`, `optimization_recommendations` callbacks (`handlers/shift_analytics*.py`)
- **Description:** В меню «👥 Смены → 📊 Аналитика и отчеты» manager-user получает callback answer `🚫 У вас нет прав доступа к этой функции` на 3 из 5 sub-actions: «Недельная аналитика», «Прогноз нагрузки», «Рекомендации по оптимизации». «Месячный отчёт» и «Анализ эффективности» — silent (no answer, no edit). Manager — целевая аудитория этого раздела.
- **Fix:** Найти `require_role(["admin"])` или подобный декоратор → расширить до `["admin", "manager"]`. Для silent-кнопок добавить error handler с alert.
- **AC:** Manager успешно открывает все 5 разделов аналитики; либо получает осмысленную ошибку, не "Нет прав".
- **Estimate:** 1.5 h.

#### BUG-BOT-004 — Executor «Нет прав» на собственной назначенной заявке
- **Priority:** P1 — **Type:** BUG / PERM — **Source:** E2E-MCP
- **Files:** `handlers/executor_requests.py` или handler для callback `view_request_NNN`
- **Description:** В режиме executor открываем «🛠 Активные заявки» — список содержит назначенные мне (DB подтверждает `executor_id = my user.id`) заявки. Click → callback answer `Нет прав для просмотра этой заявки`. Executor не может видеть детали назначенных задач — фактически блокирует выполнение работы.
- **Fix:** В permission-check добавить ветку `request.executor_id == current_user.id` (помимо `manager`/`admin`).
- **AC:** Executor открывает свою активную заявку и видит детали; чужие — продолжает получать "Нет прав".
- **Estimate:** 1 h.

#### BUG-BOT-005 — Executor «Нет прав» на собственной истории/расписании смен
- **Priority:** P1 — **Type:** BUG / PERM — **Source:** E2E-MCP
- **Files:** handlers/services для callbacks `view_week_schedule`, `shift_history` в `my_shifts_*` модуле
- **Description:** В режиме executor открываем «📋 Мои смены». Кнопки «📅 Расписание на неделю» и «📊 История смен» возвращают `🚫 У вас нет прав доступа к этой функции`. Кнопки названы «Мои», но фактически закрыты от executor.
- **Fix:** Расширить permission-фильтр до `["admin", "manager", "executor"]` с обязательным фильтром по `executor_id = current_user.id` в query.
- **AC:** Executor видит только свои смены/историю; manager — все.
- **Estimate:** 1 h.

#### BUG-BOT-006 — «🔄 Передача смен» возвращает «Ошибка загрузки меню»
- **Priority:** P1 — **Type:** BUG — **Source:** E2E-MCP
- **Files:** handler для callback `shift_transfer_menu`
- **Description:** В «📋 Мои смены → 🔄 Передача смен» — callback answer "Ошибка загрузки меню" вместо открытия меню. Функция полностью неработоспособна для executor.
- **Fix:** Trace `shift_transfer_menu` handler; вероятно отсутствует роутер или есть exception, который пробрасывается в callback answer.
- **AC:** Меню открывается; unit-test покрывает entry.
- **Estimate:** 1.5 h (включая диагностику).

#### BUG-BOT-007 — `🔥 Текущие смены` возвращает «Заявка не найдена» (неверный контекст ошибки)
- **Priority:** P2 — **Type:** BUG / UX — **Source:** E2E-MCP
- **Files:** handler для callback `view_current_shifts`
- **Description:** В «📋 Мои смены → 🔥 Текущие смены» (для executor) — callback answer "Заявка не найдена". Контекст неверный (это смена, не заявка). Возможны два варианта: (a) handler шарит фабрику с requests, (b) код ловит generic NotFound exception и подставляет нерелевантное сообщение.
- **Fix:** Заменить error-сообщение на «Активная смена не найдена» или показать сообщение об отсутствии смен.
- **AC:** Сообщение соответствует контексту; smoke-test пройден.
- **Estimate:** 30 min.

### Medium / Localization

#### BUG-BOT-008 — `MY_REQUESTS_LIST` — несогласованный шаблон заголовка пагинации
- **Priority:** P2 — **Type:** L10N — **Source:** E2E-MCP
- **Files:** `handlers/requests.py` или `keyboards/requests.py` (форматирование headers)
- **Description:** Заголовки разных страниц/фильтров используют разные шаблоны:
  - Page 1 (Все): `📋 **Все** (стр. 1/2)`
  - Page 2 (Все): `📋 Ваши заявки (страница 2/2)`
  - Активные: `📋 Активные заявки (страница 1/2):`
  - Архив: `📋 Архив заявок (страница 1/1):`
- **Fix:** Единый шаблон, например `📋 {filter_label} — страница {n}/{m}`. Унифицировать markdown.
- **AC:** Все 4 заголовка следуют одному шаблону.
- **Estimate:** 30 min.

#### BUG-BOT-009 — Raw localization key `user_management.pending_users` в UI
- **Priority:** P2 — **Type:** L10N — **Source:** E2E-MCP
- **Files:** `config/locales/ru.json` (missing key), `config/locales/uz.json`, handler для `user_mgmt_list_pending_1`
- **Description:** В «👥 Управление пользователями → 📝 Новые жители» заголовок сообщения = `user_management.pending_users\n\nСтраница 1 из 1 (всего: 1)` — raw i18n-ключ без перевода. Видно конечному пользователю.
- **Fix:** Добавить ключ в `ru.json` и `uz.json` под `user_management.pending_users` (например, "📝 Новые жители" / "📝 Yangi aholilar"). Заодно scan по `grep -r "raw key" --include="*.py"` — могут быть другие.
- **AC:** В обоих языках заголовок переведён; нет других raw-ключей в UI.
- **Estimate:** 30 min.

#### BUG-BOT-010 — Двойная иконка в кнопках (`◀️ 🔙 Назад`, `💾 💾 Сохранить`, `🔄 🔄 Обновить`)
- **Priority:** P2 — **Type:** L10N / UX — **Source:** E2E-MCP
- **Files:** `keyboards/admin/user_management.py`, `keyboards/role_management.py`, `config/locales/*.json` (i18n template уже содержит emoji)
- **Description:** В нескольких меню (USER_MGMT_ROOT, USER_PROFILE_VIEW, EMPLOYEE_MGMT_ROOT, ROLE_ASSIGN_FORM) кнопки имеют двойные иконки: i18n template для метки уже включает emoji (`🔙 Назад`), а keyboard builder добавляет ещё один (`◀️`). Результат — `◀️ 🔙 Назад`.
- **Fix:** Аудит buildery — убрать дублирующиеся emoji или из template или из builder, оставить одно место истины.
- **AC:** Кнопки имеют ровно одну иконку.
- **Estimate:** 45 min.

#### BUG-BOT-011 — `❌ Отмена` фактически выполняет «Назад» (mislabeled action)
- **Priority:** P2 — **Type:** L10N / UX — **Source:** E2E-MCP
- **Files:** `keyboards/admin/employee_management.py` (callback `employee_mgmt_main` под label `❌ Отмена`); проверить другие admin-keyboards
- **Description:** В «👷 Управление сотрудниками → 🛠️ Специализации» единственная кнопка возврата называется `❌ Отмена`, но callback = `employee_mgmt_main` (просто back-navigation). Никакого state нет, отменять нечего.
- **Fix:** Переименовать в `🔙 Назад` везде, где `Отмена` физически выполняет навигацию.
- **AC:** Кнопки `❌ Отмена` существуют только в FSM-flow для выхода из state с очисткой; в навигационных кейсах используется `🔙 Назад`.
- **Estimate:** 45 min.

#### BUG-BOT-012 — UZ-профиль показывает `кв.` (RU shorthand) вместо узбекского эквивалента
- **Priority:** P2 — **Type:** L10N — **Source:** E2E-MCP
- **Files:** `utils/address_helpers.py:localize_address()` или `services/profile_service.py:format_profile_text()`
- **Description:** При `language=uz` адрес рендерится `Yangi Olmazor, 14V, кв. 54`. Сокращение `кв.` (квартира) — русское. На UZ ожидается `xon.` / `kv.` или просто `№54`. Аналогично, проверить `д.` (дом) → UZ `uy`.
- **Fix:** Использовать i18n-строку для слотов `apartment_short`/`building_short` в `localize_address()`.
- **AC:** UZ profile показывает узбекские сокращения; RU — русские; e2e-тест на оба языка.
- **Estimate:** 45 min.

#### BUG-BOT-013 — `📊 Статистика` в Справочнике адресов — silent (callback не реагирует)
- **Priority:** P2 — **Type:** BUG — **Source:** E2E-MCP
- **Files:** handler для callback `addr_stats`
- **Description:** В «📍 Справочник адресов → 📊 Статистика» click не вызывает изменения экрана и не отдаёт callback answer. Полное отсутствие реакции.
- **Fix:** Trace `addr_stats` handler — возможно отсутствует или ловит exception без логирования.
- **AC:** Статистика показывается (количество дворов/зданий/квартир/жителей с разбивкой по статусам).
- **Estimate:** 1 h.

#### BUG-BOT-014 — Тупик: «👥 Назначение исполнителей» без кнопки Назад
- **Priority:** P2 — **Type:** UX / NAV — **Source:** E2E-MCP
- **Files:** keyboard builder для callback `shift_executor_assignment`
- **Description:** В «👥 Смены → 👥 Назначение исполнителей» отображается текст + 4 кнопки shift-управления, но **отсутствует** кнопка `🔙 Назад` в admin-panel или main menu. Если пользователь не помнит, что reply-keyboard ещё активен внизу, экран выглядит как тупик.
- **Fix:** Добавить `🔙 Назад → back_to_shifts` или `admin_menu`.
- **AC:** Любой экран admin sub-menu имеет явный путь вверх.
- **Estimate:** 15 min.

### Low / Cosmetic

#### BUG-BOT-015 — `Жители` пустой квартиры — буггие кнопки `✅ Да / ❌ Нет` без вопроса
- **Priority:** P3 — **Type:** UX — **Source:** E2E-MCP
- **Files:** `keyboards/addr/apartments.py` (residents-view builder)
- **Description:** В «📍 Справочник → ... → 🏠 Квартира → 👥 Жители» при 0 жителях показывается сообщение «Жителей не зарегистрировано» + кнопки `✅ Да` и `❌ Нет`. Сопутствующего вопроса нет, оба callback идентичны (`addr_apartment_view:1`). Очевидно, остаток от какого-то confirm-dialog.
- **Fix:** Убрать обе кнопки на empty-state, оставить только `◀️ Назад к квартире`.
- **AC:** На пустой квартире кнопки логичны.
- **Estimate:** 15 min.

#### BUG-BOT-016 — `TELEGRAM_CHANNEL_ID` дефолт-плейсхолдер → WARNING каждый startup
- **Priority:** P3 — **Type:** CFG — **Source:** E2E-MCP (live logs)
- **Files:** `.env`, `services/notification_service.py`, `main.py` lifespan
- **Description:** `.env` содержит `TELEGRAM_CHANNEL_ID=@your_notifications_channel` (placeholder). При каждом старте бот логирует `WARNING Не удалось отправить сообщение в канал: Telegram server says - Bad Request: chat not found`. Шум в логах + ложное впечатление сбоя.
- **Fix:** В `notification_service` пропускать отправку, если `TELEGRAM_CHANNEL_ID == "@your_notifications_channel"` или пустое; либо документировать в `.env.template` что значение должно быть заменено.
- **AC:** Лог чистый при пустом/placeholder значении; реальный канал отправляет уведомления.
- **Estimate:** 15 min.

#### BUG-BOT-017 — Junk-шаблоны смен в production-like dev DB (`еуые`, `test`)
- **Priority:** P3 — **Type:** DATA / OPS — **Source:** E2E-MCP
- **Files:** seed/migration, либо ручная очистка
- **Description:** В «👥 Смены → 🗂️ Управление шаблонами → 📋 Просмотр всех шаблонов» среди 10 шаблонов:
  - `❌ еуые` — случайные кириллические символы (QA-артефакт)
  - `❌ test` — generic
- **Fix:** Идентифицировать и удалить junk-шаблоны вручную или через миграцию. Если есть production экспорт — синхронизировать.
- **AC:** Список шаблонов содержит только осмысленные значения.
- **Estimate:** 15 min.

### Доп. items после расширенного обхода (round 2)

#### BUG-BOT-018 — `📅 Планировать неделю` сразу выполняет действие без confirmation
- **Priority:** P1 — **Type:** UX / DATA — **Source:** E2E-MCP round 2
- **Files:** handler для callback `plan_weekly_schedule`
- **Description:** В «👥 Смены → 📅 Планирование смен → 📅 Планировать неделю» click сразу создаёт 20 смен (наблюдалось при тесте) без confirm-dialog. Это destructive action (массовое создание расписания). Аналогичный риск: «🤖 Автопланирование на неделю/месяц», «⚡ Создать смены на завтра» — все три без подтверждения.
- **Fix:** Добавить confirm-step с превью: «Будет создано N смен на период X..Y, продолжить? [Да/Нет]».
- **AC:** Click не вызывает мутации БД до подтверждения; e2e-тест Cancel оставляет shifts unchanged.
- **Estimate:** 1 h на 4 кнопки.

#### BUG-BOT-019 — Reply-keyboard ambiguity: `📦 Архив` маршрутизируется в admin даже из executor MM
- **Priority:** P1 — **Type:** BUG / ROUTING — **Source:** E2E-MCP round 2
- **Files:** `handlers/base.py` (admin/executor router order), `handlers/requests_archive.py`, `handlers/admin_panel.py`
- **Description:** Текст `📦 Архив` присутствует в обоих reply-keyboards (executor MM и Admin Panel). После роль-switch на executor + click `📦 Архив` бот рендерит **admin archive** + admin keyboard, игнорируя `active_role`. Происходит из-за text-matching `F.text == "📦 Архив"` без проверки роли в фильтре.
- **Fix:** Добавить role-filter в обоих handlers: `F.text == "📦 Архив", role_filter(executor)` vs `F.text == "📦 Архив", role_filter(manager)`. Либо переименовать одну из кнопок (например `📦 Архив смен` для executor).
- **AC:** В executor mode click `📦 Архив` показывает архив назначенных executor-у заявок, не admin-view; reply keyboard остаётся executor.
- **Estimate:** 1.5 h.

#### BUG-BOT-020 — Stale «не указано» в PROFILE_EDIT после Cancel вложенного FSM
- **Priority:** P2 — **Type:** BUG / UX — **Source:** E2E-MCP round 2
- **Files:** handler для callback `cancel_input` + `cancel_profile_edit`, keyboard builder для `edit_profile`
- **Description:** Поток: Profile → Редактировать → click `📱 +<phone>` → FSM «Введите номер» → click `❌ Отмена` → возврат в PROFILE_EDIT. На этом экране теперь **все поля показаны как `📱 не указан` / `👤 не указано`** — хотя в DB значения целы (verified: phone=`+79013656381`, first_name/last_name=`User`). Это display bug — builder use stale state.data или возвращает пустую заглушку.
- **Fix:** В callback `cancel_input` перечитать user из DB и заново отрендерить keyboard с актуальными значениями.
- **AC:** После cancel вложенного FSM поля показывают реальные значения.
- **Estimate:** 45 min.

#### BUG-BOT-021 — Cancel в `add_apartment` FSM (из profile) ведёт в Admin Справочник адресов
- **Priority:** P2 — **Type:** BUG / NAV — **Source:** E2E-MCP round 2
- **Files:** handler для callback `cancel_apartment_selection`
- **Description:** Профиль → Редактировать → 🏠 Мои квартиры → ➕ Добавить квартиру → выбор двора → `❌ Отмена`. Вместо возврата в «Мои квартиры», бот открывает Admin → «📍 Справочник адресов» — совершенно другой контекст. Это серьёзная навигационная ошибка (особенно для не-admin пользователей был бы leak).
- **Fix:** Cancel должен возвращать в exit-state, откуда вошли в FSM (Profile → my_apartments).
- **AC:** Cancel возвращает в правильный родительский экран.
- **Estimate:** 30 min.

#### BUG-BOT-022 — `❓ Уточнить` на заявке → callback answer "Ошибка" (generic)
- **Priority:** P2 — **Type:** BUG — **Source:** E2E-MCP round 2
- **Files:** handler для callback `clarify_<NNN>` в admin request detail
- **Description:** В Admin → Новые заявки → детали заявки → `❓ Уточнить` — callback answer "Ошибка". Без контекста, ни ошибки в логах не было показано.
- **Fix:** Trace handler, добавить логирование root cause; либо реализовать недостающую логику clarify-flow.
- **AC:** Clarify FSM открывается; e2e test покрывает успешный путь.
- **Estimate:** 1.5 h.

#### BUG-BOT-023 — Employee detail показывает raw DB values (status/role/specialization)
- **Priority:** P2 — **Type:** L10N — **Source:** E2E-MCP round 2
- **Files:** handler `employee_mgmt_employee_<id>` + formatter
- **Description:** «👷 Управление сотрудниками → 👨‍💼 Менеджеры → ✅ Администратор» показывает:
  - `📊 Статус: approved` (должно быть `✅ Одобрен`)
  - `🎯 Роль: applicant, executor, manager` (должны быть locale-переведённые «Заявитель, Исполнитель, Менеджер»)
  - `🛠️ Специализация: ["plumber", "electrician", "hvac", "cleaning", "security", "maintenance", "installation", "repair", "landscaping"]` — JSON array как raw text, без перевода каждой специализации
- **Fix:** Использовать `get_text("statuses.{status}")`, `get_text("roles.{role}")`, `get_text("specializations.{spec}")` для каждого значения; присоединить через запятую.
- **AC:** Все три поля в employee detail отображают локализованные значения.
- **Estimate:** 45 min.

#### BUG-BOT-024 — Manager UI: profile of approved user — двойная иконка `✅ ✅ Одобрен`
- **Priority:** P3 — **Type:** L10N — **Source:** E2E-MCP round 2
- **Files:** template для status-display в `handlers/user_management.py` или `utils/status_display.py`
- **Description:** В user-management view одобренного пользователя статус отображается `✅ ✅ Одобрен` — двойной emoji. Аналогичный паттерн: `@не указано` (text "не указано" с префиксом `@` где username нет).
- **Fix:** Аудит format-template для status и username slot; убрать лишний emoji/prefix.
- **AC:** Status показан с одной иконкой; username slot нет `@` при отсутствии username.
- **Estimate:** 20 min.

#### BUG-BOT-025 — `🔍 Поиск` сотрудников не реагирует на введённый текст
- **Priority:** P2 — **Type:** BUG — **Source:** E2E-MCP round 2
- **Files:** handler для search в `employee_management` модуле, FSM state `EmployeeSearchStates.waiting_for_query`
- **Description:** В Employee Mgmt → 🔍 Поиск открывается prompt «Введите поисковый запрос...». После отправки текста (например, «Администратор») бот **не реагирует** — ни результатов, ни ошибки. FSM, видимо, не назначает handler на следующий message.
- **Fix:** Реализовать FSM-handler `EmployeeSearchStates.waiting_for_query` для message text.
- **AC:** Search возвращает list сотрудников, чьё имя/телефон содержит query.
- **Estimate:** 1 h.

#### BUG-BOT-026 — Месячный обзор расписания: `May 2026` (EN) в RU UI
- **Priority:** P3 — **Type:** L10N — **Source:** E2E-MCP round 2
- **Files:** formatter для schedule_month_view
- **Description:** «👥 Смены → 📅 Планирование → 📋 Просмотр расписания → 📊 Месячный обзор» — заголовок `**May 2026**` на английском. В RU UI должно быть `**Май 2026**`. Date-formatter использует `strftime("%B %Y")` с default locale.
- **Fix:** Использовать локализованный массив месяцев из `config/locales/{ru,uz}.json:months[]`.
- **AC:** RU: `Май 2026`; UZ: `May 2026` (узбекское `May` = май).
- **Estimate:** 30 min.

#### BUG-BOT-027 — `⭐ Квартира 54...` в Profile → "Заявка не найдена" (wrong context)
- **Priority:** P3 — **Type:** L10N / BUG — **Source:** E2E-MCP round 2
- **Files:** handler для callback `view_apartment:<id>`
- **Description:** Profile → Редактировать → 🏠 Мои квартиры → click `⭐ Квартира 54, ...` → callback answer "Заявка не найдена". Контекст неверный (это квартира). Аналогичен BUG-BOT-007 — shared NotFound exception/message.
- **Fix:** Контекст-specific error: «Квартира не найдена» либо реальная open-apartment view.
- **AC:** Сообщение об ошибке соответствует сущности; либо отображается детали квартиры.
- **Estimate:** 30 min.

### Items обнаруженные при integration smoke P0+P1 batch (2026-05-21)

#### BUG-BOT-028 — `ShiftAnalyticsService` использует несуществующее поле `Shift.actual_start_time`
- **Priority:** P2 — **Type:** BUG / DATA — **Source:** Integration smoke после BUG-BOT-003 fix
- **Files:** `uk_management_bot/services/shift_analytics_service.py` (queries/aggregations); `uk_management_bot/database/models/shift.py` (model definition)
- **Description:** После BUG-BOT-003 (permission fix) handler для manager-shift_analytics успешно выполняется до момента запроса к `Shift.actual_start_time` — модель не имеет такой колонки. Все 5 sub-actions (`weekly_analytics`, `monthly_analytics`, `workload_forecast`, `optimization_recommendations`, `efficiency_analysis`) падают с `AttributeError: 'Shift' object has no attribute 'actual_start_time'`. Симптом для пользователя — `❌ Ошибка аналитики`.
- **Fix:** Либо (a) добавить колонку `actual_start_time DateTime nullable=True` в модель + alembic-миграцию (если планируется feature), либо (b) переписать запросы аналитики на существующие поля (`start_time`, `end_time`, `status` транзишены). Решение зависит от business intent.
- **AC:** Все 5 analytics callbacks возвращают данные (или осмысленное "нет данных") без exception. Smoke через MCP: manager → 👥 Смены → 📊 Аналитика → каждая sub-action.
- **Estimate:** 2 h (option b — переписать на existing fields).

#### BUG-BOT-029 — `process_expired_transfers` зовёт несуществующий `NotificationService.notify_user`
- **Priority:** P3 — **Type:** BUG — **Source:** Integration smoke после BUG-BOT-002 fix
- **Files:** `uk_management_bot/services/shift_transfer_service.py` (метод `_notify_initiator_about_expired` или inline в `process_expired_transfers`); `uk_management_bot/services/notification_service.py`
- **Description:** Метод `ShiftTransferService.process_expired_transfers` успешно отмечает истёкшие передачи как `expired`, но при попытке уведомить инициатора падает: `'NotificationService' object has no attribute 'notify_user'`. Main path не блокируется (notifications best-effort), но `notified_count` всегда `0` и в логах ошибка на каждую expired transfer.
- **Fix:** Использовать существующий метод `NotificationService` (вероятно `send_notification(user_id, text)` или `notify_user_by_telegram_id`). Audit/refactor signature.
- **AC:** Live smoke: создать pending transfer с `created_at < now - 24h`, запустить `process_expired_transfers`, убедиться что `notified_count == 1`, инициатор получает Telegram-сообщение.
- **Estimate:** 30 min.

#### BUG-BOT-040 — `MetricsManager` методы помечены `async def`, но содержат только блокирующие sync SQLAlchemy запросы
- **Priority:** P3 — **Type:** ARCH / PERF / TECH-DEBT — **Source:** Code review 2026-05-21 (M4)
- **Files:** `uk_management_bot/services/metrics_manager.py:206` (`calculate_period_metrics`) + остальные methods класса
- **Description:** `async def calculate_period_metrics(...)` не содержит `await`. `self.db.query(...)` — синхронный SQLAlchemy. Объявление `async` без IO suspension вводит в заблуждение, плюс блокирует event loop на длительность запроса.
- **Fix:** Либо обернуть DB-calls в `run_in_executor`, либо мигрировать на `AsyncSession` + `await db.execute(...)`. Затрагивает весь `MetricsManager` (последовательная задача). Сейчас не критично — это P3 tech-debt.
- **AC:** Профайлинг через `asyncio.events.get_event_loop().slow_callback_duration` показывает, что метрики не блокируют loop > 100 ms.
- **Estimate:** 2 h (run_in_executor) или 6+ h (полный async).

#### BUG-BOT-039 — `localize_address` делает 2 `get_text` lookup'а, но в RU-ветке выбрасывает их (dead work)
- **Priority:** P3 — **Type:** PERF / REFACTOR — **Source:** Code review 2026-05-21 (H3)
- **Files:** `uk_management_bot/utils/address_helpers.py:26-32`
- **Description:** Lines 26-27 вызывают `get_text("address.apartment_short", ...)` и `get_text("address.building_short", ...)` БЕЗусловно. Затем RU-ветка возвращает `address` unchanged. Это dead work на каждом RU-рендере адреса.
- **Fix:** Перенести `get_text`-вызовы в `else` (UZ) ветку либо short-circuit перед ними.
- **AC:** RU branch не делает i18n lookups.
- **Estimate:** 15 min.

#### BUG-BOT-038 — `status_transitions` ключи различаются между ru.json (`"Новая"`) и uz.json (`"Yangi"`)
- **Priority:** P2 — **Type:** L10N / ARCH — **Source:** Code review 2026-05-21
- **Files:** `uk_management_bot/config/locales/ru.json` + `uz.json` секция `status_transitions`
- **Description:** В обоих locale-файлах есть `status_transitions` секция, но КЛЮЧИ верхнего уровня разные: RU использует кириллические русские статусы (`"Новая"`, `"В работе"`), UZ — узбекские (`"Yangi"`, `"Ishda"`). Любой код, делающий `get_text(f"status_transitions.{ru_status}", language="uz")`, упадёт на fallback и вернёт raw key. Не введено в этой сессии — pre-existing structural bug.
- **Fix:** Унифицировать ключи (использовать canonical английские/snake_case в обоих) с переводом в values.
- **AC:** Audit все `status_transitions` references по коду; единый ключевой набор; нет fallback'ов.
- **Estimate:** 1 h (refactor + verify).

#### BUG-BOT-037 — Оставшиеся exclusion-list prefix-match anti-patterns: `edit_`, `delete_`, `approve_`
- **Priority:** P2 → P3 (когда время будет) — **Type:** ARCH / TECH-DEBT — **Source:** Code review 2026-05-21
- **Files:** `uk_management_bot/handlers/requests.py:1755, 1798-1799, 2078`
- **Description:** Тот же anti-pattern как BUG-BOT-030/033/034:
  - `F.data.startswith("edit_") & ~F.data.startswith("edit_employee_") & ~F.data.startswith("edit_profile") & ~F.data.startswith("edit_first_name") & ~F.data.startswith("edit_last_name")` — exclusion list для `edit_`
  - `F.data.startswith("delete_") & ~F.data.startswith("delete_employee_")` — для `delete_`
  - `F.data.startswith("approve_") & ~F.data.startswith("approve_employee_") & ~F.data.startswith("approve_user_")` — для `approve_`
- **Fix:** Заменить на строгий regex `^(edit|delete|approve)_\\d{6}-\\d{3}$`.
- **AC:** Любой новый `edit_*`/`delete_*`/`approve_*` callback не перехватывается этими handlers; unit tests покрывают matching + non-matching.
- **Estimate:** 1 h (одна паттерн-замена + tests).

#### BUG-BOT-036 — `notify_user` вызывает `asyncio.run()` внутри running event loop — RuntimeError silently
- **Priority:** P1 — **Type:** BUG / SCHED — **Source:** Code review 2026-05-21 (H1)
- **Files:** `uk_management_bot/services/notification_service.py:493-499`
- **Description:** `notify_user` — sync wrapper, вызываемый из `process_expired_transfers` (BUG-BOT-002 fix). Запускается внутри aiogram/asyncio event loop (через `shift_scheduler.py`). Try/except RuntimeError fallback на `asyncio.run()` упадёт с `RuntimeError: This event loop is already running` — не может быть вызван внутри running loop. Только `loop.create_task()` path работает, но молча игнорируется при exceptions.
- **Fix:** Либо (a) сделать `notify_user` `async def`, `await send_to_user(...)`, await caller. Либо (b) использовать `asyncio.ensure_future()` без `asyncio.run()` fallback.
- **AC:** Scheduler `process_expired_transfers` job отправляет уведомления (`notified_count > 0` при наличии expired transfers); нет `RuntimeError` в логах.
- **Estimate:** 30 min.

#### BUG-BOT-035 — `cancel_clarification` callback → "Произошла ошибка" из `handle_manager_back_to_list`
- **Priority:** P3 — **Type:** BUG — **Source:** Live regression smoke 2026-05-21 (после BUG-BOT-022 verify)
- **Files:** `uk_management_bot/handlers/admin.py:1824` (handler) + `handle_manager_back_to_list` (вероятно та же или соседняя функция)
- **Description:** В admin → mview_<NNN> → ❓ Уточнить → ❌ Отмена — callback answer "Произошла ошибка". Handler `handle_cancel_clarification` ловит `cancel_clarification`, очищает state, затем вызывает `handle_manager_back_to_list(callback, db, roles, active_role, user)`. Эта вспомогательная функция падает (вероятно ожидает определённый state в FSM data или specific kwargs). Главная ошибка скрыта generic message.
- **Fix:** (a) trace exception в `handle_manager_back_to_list`, добавить `exc_info=True`; (b) либо упростить cancel: edit_text → меню заявок без `handle_manager_back_to_list` callthrough.
- **AC:** Cancel в clarify FSM возвращает в список заявок без ошибки. Smoke: admin → mview_NNN → Уточнить → Отмена → отображает list.
- **Estimate:** 30 min.

#### BUG-BOT-034 — `accept_` и `purchase_` prefix-match handlers в `requests.py`+`admin.py` — потенциальные коллизии (tech-debt)
- **Priority:** P3 — **Type:** ARCH / TECH-DEBT — **Source:** Static audit после BUG-BOT-033 fix (2026-05-21)
- **Files:** `handlers/requests.py:1845, 1973`, `handlers/admin.py:1671, 1842`
- **Description:** То же anti-pattern как BUG-BOT-030/033, но с меньшим blast radius:
  - `lambda c: c.data.startswith("accept_") and not c.data.startswith("accept_request_")` — поймёт любой будущий `accept_<X>` callback
  - `lambda c: c.data.startswith("purchase_") and not c.data.startswith("purchase_materials_")` — то же для purchase_
  - Дублирующиеся handlers в admin.py + requests.py (хотя один из них noted as "Дубликат удалён").
- **Fix:** Заменить на строгий regex `^accept_\\d{6}-\\d{3}$` / `^purchase_\\d{6}-\\d{3}$` (формат request_number).
- **AC:** Никакой `accept_*`/`purchase_*` callback, кроме `accept_<NNN>` / `purchase_<NNN>`, не попадает в эти handlers.
- **Estimate:** 30 min + unit tests.

#### BUG-BOT-033 — `view_` prefix-match handler в `requests.py` перехватывает все view_* callbacks (✅ closed 2026-05-21)
- **Priority:** P1 — **Type:** ARCH / ROUTING — **Source:** Live regression smoke после P0..P3 batch (2026-05-21)
- **Files:** `uk_management_bot/handlers/requests.py:1322` (был prefix-match + 9 exclusions)
- **Description:** То же anti-pattern что и BUG-BOT-030 для `cancel_`: `lambda c: c.data.startswith("view_") and not c.data.startswith(...)` ловил `view_current_shifts` (BUG-BOT-007 регрессия), `view_apartment:N` (BUG-BOT-027 — agent Y не заметил), `view_week_schedule` и любой другой `view_*` callback. После try-extract `request_number = "current_shifts"` → DB lookup fails → "Заявка не найдена".
- **Fix:** Заменил prefix-match на строгий regex `F.data.regexp(r"^view(?:_request)?_\\d{6}-\\d{3}$")` — только реальный формат `YYMMDD-NNN` матчится. Удалён exclusion list.
- **Status:** **closed** commit `7c1ba4c` (merged 2026-05-21).
- **Verification:** Live MCP smoke: executor → 📋 Мои смены → 🔥 Текущие смены → "У вас нет запланированных смен" (раньше "Заявка не найдена"). 

#### BUG-BOT-032 — F-string placeholders не отрезолвлены в weekly_analytics report
- **Priority:** P2 — **Type:** BUG / L10N — **Source:** Integration smoke после BUG-BOT-031 fix (2026-05-21)
- **Files:** `uk_management_bot/handlers/shift_management.py` (handler для `weekly_analytics`), либо template-строка в `config/locales/{ru,uz}.json`
- **Description:** После BUG-BOT-031 fix (MetricsManager.calculate_period_metrics реализован) weekly_analytics возвращает реальные данные ("Всего смен: 40"), но в выводе видны нерезолвленные template-placeholder'ы: `{avg_efficiency:.1f}%`, `{completion_rate:.1f}%`, `{on_time_rate:.1f}%`, `{assignment_rate:.1f}%`, `{avg_duration:.1f}ч`. Похоже handler берёт template-строку и НЕ применяет `.format(**metrics)` / f-string подстановку.
- **Fix:** Найти template в локали или string literal, добавить `.format(**metrics)` либо использовать .format с keyword-args. Также проверить что `metrics` dict содержит все ожидаемые ключи.
- **AC:** `weekly_analytics` rendered без `{...}` literal — все числа отображаются.
- **Estimate:** 30 min.

#### BUG-BOT-031 — `MetricsManager.calculate_period_metrics` отсутствует — раскрыт после BUG-BOT-028 fix
- **Priority:** P2 — **Type:** BUG / DATA — **Source:** Integration smoke после BUG-BOT-028 fix (2026-05-21)
- **Files:** `uk_management_bot/services/shift_analytics_service.py` или `services/metrics_manager.py`
- **Description:** После BUG-BOT-028 (заменили `Shift.actual_start_time` на `start_time` в `shift_planning_service.py`) запуск `weekly_analytics` падает на следующем layer: `'MetricsManager' object has no attribute 'calculate_period_metrics'`. Метод вызывается в shift_analytics codepath, но не реализован в MetricsManager.
- **Fix:** Либо (a) реализовать `calculate_period_metrics(period_start, period_end) -> dict` в `MetricsManager` (агрегации по периоду); либо (b) заменить вызовы на существующие методы. Audit usages: `grep -rn "calculate_period_metrics" uk_management_bot/`.
- **AC:** `weekly_analytics` callback не падает с AttributeError; возвращает данные или осмысленное "Недостаточно данных".
- **Estimate:** 2 h.

#### BUG-BOT-030 — `requests.py:2030` глобально перехватывает все `cancel_*` callbacks через `startswith`
- **Priority:** P3 — **Type:** ARCH / TECH-DEBT — **Source:** Integration smoke (root cause BUG-BOT-018 cancel)
- **Files:** `uk_management_bot/handlers/requests.py:2030` (filter с `F.data.startswith("cancel_")` + maintained exclusion list)
- **Description:** Глобальный handler для отмены заявки использует префиксный matcher и поддерживает белый список исключений (`cancel_document_selection_`, `cancel_action`, `cancel_apartment_selection`, + добавлены `cancel_plan_*` и `cancel_auto_plan_*`). При добавлении любого нового feature с callback `cancel_*` без обновления exclusion list возникает silent collision: новый handler не срабатывает, отрабатывает global. Это анти-паттерн (open-set вместо явного opt-in).
- **Fix:** Заменить prefix-match на строгий формат — например, `cancel_<request_number>` matcher через regex `^cancel_\\d{6}-\\d{3}$` (формат `YYMMDD-NNN`). Тогда любой другой `cancel_*` callback просто не матчится, никаких exclusion-listов не нужно.
- **AC:** (a) global handler матчит ТОЛЬКО `cancel_YYMMDD-NNN` формат; (b) все ранее работавшие cancel-callbacks для request_number продолжают работать; (c) удалены exclusion list; (d) unit test покрывает оба пути.
- **Estimate:** 1 h.

---

## Итоговая статистика после patch

### Текущее состояние (2026-05-22, после merge ARCH-014 PR #23 — поглотил ARCH-016 + ARCH-102)

| Уровень | Total | ✅ Merged | 🟢 Dev-closed (prod-pending) | 🔓 Open | Прим. |
|---|---:|---:|---:|---:|---|
| **P0** | 8 | 6 | 2 (FIX-002, FIX-004 — prod-pending) | 0 | Merged: BUG-BOT-001, FIX-001/003/005/006, FIX-007 (PR #24 `1a95c00` + post-merge ревью-фиксы). FIX-002 dev rotated, FIX-004 dev applied (Step 6 deferred). FIX-008 → P1 |
| **P1** | 43 | 11 | 0 | 32 | Merged: BUG-BOT-002..006, 018, 019, 033 + ARCH-014 + ARCH-016 + ARCH-011 (resolved `a20686f`, downgrade P1→P3). Filed open: BUG-BOT-036, OPS-112, ARCH-113, ARCH-114. FIX-008 принят из P0. 29 от initial audit |
| **P2** | 64 | 16 | 0 | 48 | Merged: BUG-BOT-007..014, 020..023, 025, 028, 031, 032. Filed open: BUG-BOT-037, 038 |
| **P3** | 49 | 9 | 0 | 40 | Merged/closed: BUG-BOT-015, 016, 024, 026, 027, 029, 030 + ARCH-102 + TEST-116 (PR #24). Open: BUG-BOT-017 (data), 034, 035, 039, 040, SEC-115, OPS-117, FE-119 |
| **Подсумма Priority-помеченных** | 164 | 42 | 2 | 120 | verified `grep -cE "^- \\*\\*Priority:\\*\\* +P[0-9]"` = 161 std + 3 не-стандарт-формат (FIX-008/ARCH-011 strikethrough, TEST-069 double-bold) = 164 (+OPS-112, +ARCH-113/114, +SEC-115, +TEST-116, +OPS-117, +FE-119) |
| Merged-marker (cross-ref dupes) | −3 | — | — | — | ARCH-101, ARCH-102, NICE-080 — sub-tasks parent issue'ев |
| **Effective active items** | 161 | 39 | 2 | 120 | для распределения нагрузки и времени |
| Omitted (с обоснованием) | 1 | — | — | — | NOTE-S-M7 |
| **Total entries в файле** | 165 | — | — | — | (161 effective + 3 cross-ref dupes + 1 omitted) |

**Math check:** 42 merged + 2 dev-closed + 120 open = 164 ✓ (dev-closed FIX-002/004 одновременно awaiting prod user-action — те же 2 items). Effective: 39 + 2 + 120 = 161 ✓. File entries: 161 + 3 + 1 = 165 ✓.

**P0 release gate state:** 8 of 8 closed/dev-closed. Remaining: 2 prod user-action (FIX-002, FIX-004). Code-level P0 delivery to main complete (BUG-BOT-001 + FIX-001/003/005/006 + FIX-007 PR #24). Releasable после FIX-002/FIX-004 prod execution.

### Session deliverables (2026-05-21)

| Метрика | Значение |
|---|---:|
| BUG-BOT items merged to main | **33** (001..033 исключая 017, 034, 035) |
| Items newly filed (in-session discoveries) | **10** (BUG-BOT-031..040; из них 031, 032, 033 merged; 034..040 open) |
| Commits на `origin/main` | 56 |
| Tests added | ~182 unit + ~30 MCP smoke verifications |
| Branches: created → merged → deleted | 9 fix branches + 5 worktree branches |
| Coordinator patches | 3 (logger compat, cancel routing exclusion, view routing regex) |
| Code review | code-reviewer subagent: 0 critical, 3 high, 4 medium, 3 low → 5 new items filed (036..040) |
| Live MCP smoke | 30+ menu interactions verified post-merge |

### Closed Items Registry → see beginning of file

Полный mapping `(ID → Priority → Fix commit → Branch)` — секция **`## Closed Items Registry`** в начале файла.

**Оценка оставшейся работы:** ~115 effective open items. Грубо ~4 недели full-time для одного разработчика (mix L10N quick fixes + ARCH/SEC/TEST крупные items).

---

## Распределение по типам (multi-label)

**Метод:** composite-тип вида `SEC / ARCH` учитывается **в обеих** категориях (multi-label). Например, ARCH-101 (Type: `SEC / ARCH`) добавляет +1 к SEC и +1 к ARCH. Сумма всех категорий поэтому **больше** числа entries — это by design.

Cross-check данные получены через `grep -oE "\*\*Type:\*\* +[A-Z/ ]+" | tr '/' '\n' | sort | uniq -c`:

| Type | Кол-во меток |
|---|---|
| SEC | 29 |
| REFACTOR | 22 |
| DB | 15 |
| BUG | 14 |
| ARCH | 13 |
| PERF | 11 |
| UX | 9 |
| OPS | 6 |
| TEST | 4 |
| INV | 1 |
| I18N | 1 |
| DOCS | 1 |
| **Total labels** | **126** |

126 меток на 106 entries: ~20 пунктов имеют composite-тип. Для планирования итерации использовать **`Priority:` фильтр**, не Type-distribution.

---

## Cross-check verdict

`general-purpose` агент-верификатор: **NEEDS-PATCH** → patch применён (3 missing items добавлены, дубликаты помечены как merged, S(M-7) явно опущен с обоснованием).

После применения patch:
- Все 8 CRITICAL и 30+ HIGH из full-review покрыты.
- Все MEDIUM из full-review покрыты (включая ранее пропущенные DB triggers и apartments.area).
- Дубликаты явно cross-referenced.
- Статистика согласована с фактическим содержимым файла.

---

## Ссылки

**Tracked в git (стабильные):**

- Полный отчёт: `docs/audit/2026-05-20-full-review.md`
- E2E тест-спеки: `tests/e2e/specs/`

**Ephemeral (не воспроизводимы после checkout — `*.png` в `.gitignore:166`, temp-файлы host-specific):**

- E2E скриншоты: `tests/e2e/artifacts/*.png` — gitignored
- Скриншот pair_with_next: `board-paired.png` — gitignored
- Backend pytest output: был на `/private/tmp/claude-501/.../tasks/bq8kyol9b.output`; для повторного прогона см. секцию "Артефакты этой сессии" в full-review.md.

Если CI-pipeline нужен — рекомендуется добавить exception в `.gitignore` для `tests/e2e/artifacts/` либо аплоадить артефакты через CI provider (GitHub Actions `actions/upload-artifact`).
