# FIX-003 — Verifier Log

> _Последнее редактирование: 2026-05-21_

**Item:** FIX-003 — `requests.apartment_id` FK блокирует purge
**Priority:** P0  •  **Type:** DB / BUG  •  **Branch:** `fix/fix-003-fk-purge-set-null`
**Worker:** uk-worker-fix-003 (general-purpose, sonnet)  •  **Closed:** 2026-05-21

## Deliverables

- `alembic/versions/007_set_null_on_request_apartment_fk.py` (new)
- `uk_management_bot/tests/test_apartment_purge.py` (new)

## FK discovery (pre-state)

| Constraint | confdeltype | Meaning |
|---|---|---|
| `requests_apartment_id_fkey` | `a` | NO ACTION (broken behaviour) |

`requests.apartment_id` already nullable — no column-level alter needed.

## Verifier 1 — test-runner — PASS (11/11 gates)

| Gate | Result |
|---|---|
| 1. Container Up | PASS |
| 2. docker cp | PASS |
| 3. alembic current → downgrade | PASS |
| 4. Pre-state FK confdeltype='a' | PASS |
| 5. RED test pre-migration (FK violation) | PASS |
| 6. `alembic upgrade head` | PASS |
| 7. Post-upgrade FK confdeltype='n' | PASS |
| 8. GREEN test post-migration | PASS |
| 9. Downgrade -1 reverts FK | PASS |
| 10. Re-upgrade reapplies FK | PASS |
| 11. Regression sweep (195 passed vs 192 baseline, +3 = FIX-001+003 new tests, 0 fail) | PASS |

Confidence: HIGH

## Verifier 2 — ac-checker — PASS

AC verbatim: «Удаление apartment с историческими requests не падает, `requests.apartment_id` становится NULL; покрыто тестом `test_apartment_purge_keeps_request_history`.»

- (a) No FK violation on delete: PASS (migration ondelete='SET NULL' + test asserts no IntegrityError)
- (b) apartment_id IS NULL after delete: PASS (test assertion at line 156)
- (c) Test name exact match: PASS (`test_apartment_purge_keeps_request_history`)
- (d) Migration reversible: PASS (round-trip verified by test-runner)

`AC verdict: PASS`

## Verifier 3 — code-reviewer (python-reviewer) — PASS-WITH-CONCERNS

| Severity | Finding | Resolution |
|---|---|---|
| HIGH | `role` column inserted (CLAUDE.md violates "Не использовать user.role") | **Fixed** — added `active_role`, added comment explaining `role` NOT NULL constraint forces inclusion; tracked schema-level server_default as future work |
| MEDIUM | request_number format `YYMMDD-TXXX` deviates from canonical `YYMMDD-NNN` | **Fixed** — canonical `YYMMDD-NNN` with 900-999 high-range to reduce production collision |
| LOW | `sessionmaker(bind=engine)` deprecated kwarg in SQLAlchemy 2.x | Deferred — pre-existing project SQLAlchemy upgrade is broader REFACTOR scope |
| LOW | Teardown has no error handling on partial setup failure | Deferred — acceptable for integration test against real DB; CI isolation issue |

Migration itself: zero issues across all 7 review dimensions.

Re-verified after fixture cleanup: 1/1 PASS.

## Convergence verdict

3 verifiers PASS (one with concerns, HIGH+MEDIUM resolved post-review). Item closed.

## Production deployment notes (coordinator follow-up)

- Migration acquires ACCESS EXCLUSIVE on `requests` for the DROP/ADD CONSTRAINT — brief downtime on busy systems. Schedule during low-traffic window.
- Container rebuild required: migration file baked into image per FIX-001 caveat.
- `user_apartments.apartment_id` already has ON DELETE CASCADE — apartment purge will delete user-apartment association rows. No change needed (out of FIX-003 scope).
- All app-level reads of `request.apartment_id` are already None-safe (verified by code-reviewer).

## Followup INV candidates (logged in memory)

- Schema-level `server_default='applicant'` for `users.role` column → less ceremony in raw-SQL test fixtures
- Format check constraint on `request_number` to enforce `YYMMDD-NNN`
