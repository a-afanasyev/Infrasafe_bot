# FIX-001 — Verifier Logs

> _Последнее редактирование: 2026-05-21_

**Item:** FIX-001 — `NameError: user_data` в invite-регистрации
**Priority:** P0  •  **Type:** BUG  •  **Branch:** `fix/fix-001-invite-namerror`
**Worker:** uk-worker-fix-001 (general-purpose, sonnet)  •  **Closed:** 2026-05-21

## Diff (source)

```diff
--- a/uk_management_bot/web/api/invite.py
+++ b/uk_management_bot/web/api/invite.py
@@ -119,8 +119,8 @@ async def register_via_invite(request: Request, data: RegistrationData, db: Sess
         result = invite_service.join_via_invite(
             token=data.token,
             telegram_id=data.telegram_id,
-            first_name=user_data["first_name"],
-            last_name=user_data["last_name"],
+            first_name=data.full_name.split()[0] if data.full_name else "",
+            last_name=" ".join(data.full_name.split()[1:]) if len(data.full_name.split()) > 1 else "",
             specialization=data.specialization
         )
```

## New test

`uk_management_bot/tests/test_invite_register.py` — 3 tests covering new-user happy path, single-name, multi-word last-name. Bypasses TEST-069 legacy-import surface via direct async-call + mocked InviteService/AuthService.

## Verifier 1 — test-runner — PASS

- Container `uk-management-bot` Up + healthy
- `docker cp` (no bind mount — source baked in container)
- New tests: **3/3 PASSED**
- Broader sweep (`pytest uk_management_bot/tests/ -q --ignore=…`): **192 PASSED, 0 failed** (5 pre-existing deprecation warnings, no net-new regressions)
- Confidence: HIGH

## Verifier 2 — ac-checker — PASS

AC verbatim quoted: «POST /api/register с новым telegram_id отдаёт 200 и пользователь появляется в users … Полный pytest-прогон test_invite_integration — отдельная задача TEST-069, не входит в AC этого фикса.»

- (a) POST returns 200 — PASS (invite.py:119-125 + 130-134; new tests assert dict + kwargs)
- (b) User appears in `users` — PASS (`invite_service.join_via_invite` does `db.add(user); commit()` at invite_service.py:367-379)
- (c) test_invite_integration out of scope — PASS (new test bypasses legacy-import surface)

`AC verdict: PASS`

## Verifier 3 — code-reviewer (python-reviewer) — PASS-WITH-CONCERNS

| Dimension | Rating |
|---|---|
| Correctness | PASS — exact mirror of lines 105-106 |
| Minimal-change | PASS — only 2 lines |
| PEP 8 / type hints | CONCERN — dead `if False else` branch in test helper (test_invite_register.py:38-39) |
| Test quality | CONCERN — missing `full_name=""` test |
| Hidden risks | CONCERN — pre-existing whitespace-only IndexError (also in lines 105-106; not introduced) |
| Security | PASS — decorators intact |
| Over-engineering | PASS |

Coordinator action on concerns:
1. **Dead branch in `_run` helper** — fixed (1-line cleanup; re-verified 3/3 green).
2. **Missing empty/whitespace tests** — deferred, not in AC.
3. **Whitespace IndexError pre-existing bug** — logged as INV candidate (see `uk:observation:full_name-whitespace-bug` in memory namespace `uk-audit-2026-05-20`).

## Convergence verdict

3 verifiers PASS. Item closed.

## Container deployment note (coordinator follow-up)

The container `uk-management-bot` does NOT bind-mount source — verifier used `docker cp` to ship patched files into the running container. **Production rebuild required before runtime takes effect:**

```bash
docker compose build uk-management-bot && docker compose up -d uk-management-bot
```

This is tracked in the final execution report; do NOT skip when promoting the FIX-001 branch.
