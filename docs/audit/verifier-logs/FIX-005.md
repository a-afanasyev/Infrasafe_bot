# FIX-005 — Verifier Log

> _Последнее редактирование: 2026-05-21_

**Item:** FIX-005 — Webhook HTTP 503 помечен `retryable=False`
**Priority:** P0  •  **Type:** BUG  •  **Branch:** `fix/fix-005-webhook-503-retryable`
**Worker:** uk-worker-fix-005 (general-purpose, sonnet)  •  **Closed:** 2026-05-21

## Diff

```diff
- return (False, f"HTTP 503: service unavailable", False, 0)
+ return (False, "HTTP 503: service unavailable", True, 0)
```

Single line in `uk_management_bot/services/webhook_sender.py:189`. Flag `retryable` flipped `False→True`; redundant f-string prefix dropped. 503 now consistent with general 5XX branch on next line.

## New tests

`uk_management_bot/tests/test_webhook_503_retry.py` (new). 2 tests:
- `test_single_503_keeps_record_pending_and_schedules_retry` — one cycle → status=pending, attempts=1, retry_after set
- `test_503_goes_to_failed_only_after_max_retries` — N-1 cycles pending, N-th cycle failed

## Verifier 1 — test-runner — PASS

| Gate | Result |
|---|---|
| Container Up | PASS |
| docker cp source + test | PASS |
| New tests (2 expected) | PASS — 2/2 |
| Regression test_webhook_sender.py | PASS — 7/7 (existing) |
| Broader sweep (≥195 baseline + FIX-003 add = 196) | PASS — 196/196 |

Confidence HIGH.

## Verifier 2 — ac-checker — PASS

AC verbatim verified. 3 bullets graded:
- (a) `httpx.Response(503)` → pending/attempts=1/retry_after set after one cycle — PASS (test:122-140)
- (b) Field name `attempts` (NOT `retry_count`) — PASS (test:101, 138, 171, 177)
- (c) Goes to `failed` only when `attempts >= MAX_RETRIES` — PASS (test:164-181 with MAX=3 mock)

`AC verdict: PASS`

## Verifier 3 — code-reviewer (python-reviewer) — PASS-WITH-CONCERNS

| Dimension | Rating |
|---|---|
| Correctness | PASS — 503 now retryable, consistent with `>=500` branch |
| Minimal-change | PASS — 1 line + cosmetic f-string drop |
| PEP 8 / project style | PASS |
| Test quality | PASS |
| Hidden risk (retry_after=0 semantics) | PASS — `> 0` guard in process_outbox makes 0 → default backoff (verified at webhook_sender.py:279-283) |
| Security | PASS |
| Over-engineering | PASS |

**MEDIUM cosmetic concern:** nested `_clear_retry_after` helper at test:154 could be module-level for consistency. **Deferred** — pure style churn, not worth a patch round.

`Reviewer verdict: PASS-WITH-CONCERNS — cosmetic only`

## Convergence verdict

3 verifiers PASS. Item closed.

## Related followups

- `retry_after=0` returning the default-backoff path was the right interpretation; if backlog later asks for explicit "use Retry-After header" for 503, that's a separate enhancement.
