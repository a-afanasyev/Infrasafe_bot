# FIX-002 — Verifier Log

> _Последнее редактирование: 2026-05-21_

**Item:** FIX-002 — Ротация всех живых секретов на диске
**Priority:** P0  •  **Type:** SEC  •  **Branch:** `fix/fix-002-secret-rotation`
**Status:** **awaiting-user-action** (runbook готов; rotation сам по себе требует @BotFather + ручной правки `.env`)
**Closed-as-prepared:** 2026-05-21

## Deliverable

`docs/audit/runbooks/FIX-002.md` — пошаговый runbook на 30 минут ручной работы для пользователя.

## Pre-flight evidence (без вывода значений)

| Файл | Tracked? | Target-keys count | Заметка |
|---|:-:|:-:|---|
| `.env` (root) | gitignored | 6 of 7 target keys | `UK_WEBHOOK_SECRET` отсутствует — живёт в `settings.py:99-100` |
| `uk_management_bot/.env` | gitignored | 3 of 7 (BOT_TOKEN, INVITE_SECRET, ADMIN_PASSWORD) | |
| `media_service/.env` | gitignored | 0 of 7 — использует `TELEGRAM_BOT_TOKEN` (не `MEDIA_BOT_TOKEN`) | |

## Discrepancies с backlog (зафиксированы в runbook)

1. `MEDIA_BOT_TOKEN` больше не дублируется — только в root `.env` (backlog описывал прошлое состояние)
2. `media_service/.env` использует имя `TELEGRAM_BOT_TOKEN` — unclear, тот же токен или другой
3. `UK_WEBHOOK_SECRET` отсутствует во всех `.env` — живёт в `settings.py` defaults (документировано как Discrepancy #2 в runbook)

## Verifier — ac-checker (qa-analyst) — PASS

| Bullet | Verdict | Evidence |
|---|---|---|
| (a) Все 8 значений покрыты | PASS | runbook inventory table FIX-002.md:12-21 |
| (b) Старые BOT-токены revoked в @BotFather | PASS | Step 1 + curl getMe verification (Step 4) |
| (c) Новые значения никогда в git | PASS | `git status` + `git check-ignore` проверки (Step 4) |
| (d) `grep` AC команда применена | PASS | FIX-002.md:122 (расширена `MEDIA_BOT_TOKEN`/`TELEGRAM_BOT_TOKEN` — defensible улучшение) |
| (e) Coordinator caveats применены | PASS | SEC-083 raw-chars (≥16); FIX-007 still-rotate (объяснено) |

**Дополнительные гигиенические проверки:** backup/rollback ✓, container restart ✓, smoke-checks ✓, discrepancy surfacing ✓, signal-completion protocol ✓.

**Minor nits применены к runbook post-review:**
- ✓ Base64 special-chars caveat для HMAC-секретов (вопрос URL-traversing — не их случай, но flag добавлен)
- ✓ `docker logs uk-media-service` смок-команда в Step 5
- ✓ Quoting спецсимволов в alt password generation
- ✓ `getMe` disambiguation curl-команда добавлена в Step 1 (раньше была только в Step 4)

## Convergence verdict

`AC verdict: PASS` — runbook ready. **Status:** `awaiting-user-action`. Координатор не закрывает item автоматически. Ждёт сигнал `FIX-002 rotated` или `FIX-002 blocked: <reason>` от пользователя.

## Next coordinator action

Продолжить с **FIX-003** (FK migration apartment purge) параллельно — FIX-002 не блокирует FIX-003. FIX-002 закроется после пользовательского сигнала.
