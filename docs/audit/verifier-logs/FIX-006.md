# FIX-006 — Verifier Log

> _Последнее редактирование: 2026-05-21_

**Item:** FIX-006 — Invite-токен в plaintext в логах
**Priority:** P0  •  **Type:** SEC  •  **Branch:** `fix/fix-006-invite-token-log-mask`
**Worker:** uk-worker-fix-006 (general-purpose, sonnet)  •  **Closed:** 2026-05-21

## Diff

Two log statements masked in `uk_management_bot/handlers/auth.py`:

```diff
@@ Exit log (originally line 172, AC-prescribed fix)
- logger.info(f"Пользователь {telegram_id} получил ссылку на веб-регистрацию с токеном {token}")
+ logger.info(f"Пользователь {telegram_id} получил ссылку на веб-регистрацию с токеном {token[:8]}…")

@@ Entry log (originally line 66 — coordinator-extended for AC compliance)
- logger.info(f"Команда /join получена от пользователя {message.from_user.id}: {message.text}")
+ _join_parts = (message.text or "").split(maxsplit=1)
+ _token_arg = _join_parts[1] if len(_join_parts) > 1 else ""
+ _masked = f"{_token_arg[:8]}…" if _token_arg else "(empty)"
+ logger.info(f"Команда /join получена от пользователя {message.from_user.id}: /join {_masked}")

@@ Pre-existing split (post-review consistency patch)
- text_parts = message.text.split(maxsplit=1)
+ text_parts = (message.text or "").split(maxsplit=1)
```

## Scope-extension rationale (vs backlog literal Fix)

Backlog Fix described only line 172. But the AC says
`docker logs ... | grep -E "invite_v1:[A-Za-z0-9_-]{20,}" → пусто`.
Without masking line 66, every `/join <token>` invocation would still
match that grep. Worker correctly flagged line 66 as **out of literal
scope but in AC scope**. Coordinator extended the fix.

## New tests

`uk_management_bot/tests/test_invite_token_logging.py` (new, 2 tests):
- `test_join_handler_masks_token_in_log` — narrow line-172 assertion
- `test_join_handler_no_full_token_in_any_log` — **AC-level**: aggregates ALL log records from `uk_management_bot.handlers.auth` logger, asserts full token body absent

## Verifier 1 — test-runner — PASS (6/6 gates)

| Gate | Result |
|---|---|
| Container Up | PASS |
| docker cp source + test | PASS |
| Both fix patterns grep-present in patched source | PASS |
| 2 new tests | PASS — 2/2 |
| Broader regression sweep | PASS — 198 passed |

Confidence HIGH.

## Verifier 2 — ac-checker — PASS

AC verbatim verified. Both log statements masked. AC-level test (`test_join_handler_no_full_token_in_any_log`) asserts NO log record from the handler contains the full token body — directly mirrors AC grep semantics.

Verifier specifically traced:
- Line 71 (masked entry): `Команда /join … /join invite_v…` — does NOT match `invite_v1:[A-Za-z0-9_-]{20,}` regex
- Line 177 (masked exit): same shape, also does NOT match

`AC verdict: PASS`

Caveat surfaced: line 109 `logger.info(f"Невалидный токен от {telegram_id}: {e}")` — coordinator audited InviteService.validate_invite error messages, **none embed the raw token**, so {e} is safe. Documented for REFACTOR-032 audit completeness.

## Verifier 3 — code-reviewer (python-reviewer) — WARNING (4 concerns, 1 patched, 3 deferred)

| Dimension | Rating | Resolution |
|---|---|---|
| Correctness (1a-1d) | PASS (1c minor) | edge case "very short token" — invite_v1: scheme always >8 chars, not realistic |
| Minimal-change / duplicate split | CONCERN | **Deferred** — DRY violation between line 68 and line 86 is real but consolidating means broader handler refactor; cost > benefit for FIX-006 |
| PEP 8 / style | PASS | — |
| Test quality | CONCERN | Exception path (line 109) not exercised — **resolved by audit** (no token leak in validate_invite errors) |
| Hidden risks (line 86 None-safety) | CONCERN | **Patched** — added `(message.text or "")` guard for consistency with line 68 fix |
| Security | PASS | operator can still correlate by prefix |
| Line 109 exception leak | CONCERN | **Resolved by audit** — InviteService validate_invite error messages (invite_service.py:161-219) use generic strings, no token interpolation |

`Reviewer verdict: WARNING — no CRITICAL/BLOCK. 4 CONCERNs: 1 patched (line 86 None-guard), 3 deferred-with-justification (DRY duplicate-split, exception path test coverage, prefix-correlation usability).`

Re-verified post-patch: 2 target tests + 56 handler smoke tests = 58/58 PASS.

## Convergence verdict

3 verifiers PASS / PASS / WARNING-resolved. Item closed.

## Followup INV candidates

- **REFACTOR-032 cross-reference**: this fix addressed 2 sites (entry + exit logs in /join handler). REFACTOR-032 scope is **broader logger audit** — needs to cover error paths, FSM transitions, and forwarded-message handling.
- **prefix-correlation usability**: every masked log shows `invite_v…` (no discriminator). Consider hashed-prefix or last-4-chars in REFACTOR-032.
- **DRY**: two `message.text.split(maxsplit=1)` calls in same handler — out of FIX-006 minimal scope, but worth tracking for REFACTOR-032.
