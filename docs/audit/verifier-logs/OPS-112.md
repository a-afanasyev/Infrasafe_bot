# OPS-112 — Prod verification of webhook-hardening (8 checks)

> _Последнее редактирование: 2026-05-31_

**Spec:** `docs/superpowers/specs/2026-05-19-uk-infrasafe-webhook-hardening.md` (Phase 3 Verification)
**Run:** 2026-05-31, prod `95.46.96.105` (`uk-management-api`, `infrasafe-app-1`, `infrasafe-postgres-1`)
**Verdict:** ✅ **All 8 checks PASS.** OPS-112 closed.

> Context: this run also confirmed the resolution of the 2026-05-30 outbound-401 incident (UK↔InfraSafe `INFRASAFE_WEBHOOK_SECRET` mismatch after rotation; UK realigned to InfraSafe value). Secrets are matched (`609bab65…` both sides, verified by sha256 prefix), 0 new 401 after the fix.

| # | Check | Verdict | Evidence |
|---|-------|---------|----------|
| 1 | Settings sanity | ✅ PASS | `INFRASAFE_WEBHOOK_ENABLED = True`; `REDIS_PUBSUB_URL has password = True` (PR-B derive working); `INFRASAFE_WEBHOOK_URL = https://infrasafe.uz` |
| 2 | `/uk/api/health/outbox` returns JSON | ✅ PASS | `{"enabled":true,"pending":0,"oldest_pending_age_sec":0,"failed_last_24h":464}` (PR-C endpoint live; `failed_last_24h` is historical from the 401 window, decays out of the 24h window) |
| 3 | `process_outbox cycle` log | ✅ PASS | `process_outbox` has `if not records: return` **before** the cycle log (`webhook_sender.py:~261`), so empty cycles (pending=0) don't log — matches spec note "пустые циклы могут не логироваться". Cycle log fired under load during the 2026-05-30 reconcile replay (50 events) |
| 4 | UK building → InfraSafe (delivery) | ✅ PASS | Both UK buildings present in InfraSafe `buildings` with matching `external_id` (see #5). Live delivery also proven by reconcile replay: 50 events created post-fix all `status=sent, attempts=1`, 0 × HTTP 401 |
| 5 | Deterministic `external_id` (CR-2) | ✅ PASS | UK-predicted (`reconciliation._expected_external_id`) **==** InfraSafe-stored, exact match: UK id=5 → `f2925f03-3cba-c237-3196-e4d6b03e8239` (InfraSafe building_id=5 ✓); UK id=3 → `b3f1d316-842c-598e-52e4-50316e1727fa` (InfraSafe building_id=6 ✓). InfraSafe derives via `sha256("uk-building-{id}")` (`Infrasafe/src/services/uk/buildingSync.js:37`) |
| 6 | No duplicate sends (PR-A race) | ✅ PASS | Duplicate **by `event_id`** (the true race/idempotency key) in `integration_log` `direction='from_uk'` → **0 rows**. NB: grouping by `entity_id` (spec's original query) shows COUNT>1, but that is *legitimate* — a request emits multiple `status_changed` events over its lifecycle + reconcile re-enqueues idempotently, each with a fresh `event_id` (ARCH-010). SKIP LOCKED prevents the actual race; sent rows show `attempts=1` |
| 7 | No Redis auth errors | ✅ PASS | `docker logs --since 15m uk-management-api \| grep -ci "redis.*auth\|AuthenticationError"` → **0** (PR-B `REDIS_PUBSUB_URL_RESOLVED` carries auth) |
| 8 | Reconciliation running | ✅ PASS | `reconcile_buildings: precise diff — uk=2 is=0 missing=2 enqueued=2 orphans=0`; `reconcile_requests: uk=29 is=5 missing=24 enqueued=24 orphans=0` (PR-D loop active; advisory-lock guarded). `is=5` for requests is the known ARCH-114 metric scope — InfraSafe `uk-requests-metrics` surfaces only ARM-mapped requests — not a delivery gap |

## Commands (reproducible, secrets masked)

```bash
SSH="ssh -p 32323 infrasafe@95.46.96.105"

# 1
$SSH 'docker exec uk-management-api python -c "from uk_management_bot.config.settings import settings as s; print(s.INFRASAFE_WEBHOOK_ENABLED, \"@\" in (s.REDIS_PUBSUB_URL_RESOLVED or \"\"))"'
# 2
curl -sk https://95.46.96.105/uk/api/health/outbox
# 3
$SSH 'docker logs --since 40m uk-management-api 2>&1 | grep "process_outbox cycle"'   # empty when pending=0 (if not records: return)
# 5 (UK predict)
$SSH 'docker exec uk-management-api python -c "from uk_management_bot.database.session import SessionLocal as S; from uk_management_bot.database.models.building import Building as B; from uk_management_bot.services.reconciliation import _expected_external_id as e; d=S(); [print(b.id, e(b.id)) for b in d.query(B).all()]"'
# 5 (InfraSafe stored) / 4
$SSH 'docker exec infrasafe-postgres-1 psql -U infrasafe_app -d infrasafe -tAc "SELECT building_id, external_id, name FROM buildings WHERE external_id IS NOT NULL ORDER BY building_id DESC LIMIT 8;"'
# 6 (precise — by event_id, expect 0)
$SSH 'docker exec infrasafe-postgres-1 psql -U infrasafe_app -d infrasafe -tAc "SELECT event_id, COUNT(*) FROM integration_log WHERE direction='"'"'from_uk'"'"' AND event_id IS NOT NULL GROUP BY event_id HAVING COUNT(*)>1 LIMIT 10;"'
# 7
$SSH 'docker logs --since 15m uk-management-api 2>&1 | grep -ci "redis.*auth\|AuthenticationError"'
# 8
$SSH 'docker logs uk-management-api 2>&1 | grep -iE "reconcile_(buildings|requests):" | tail -2'
```

## Notes / follow-ups (non-blocking)
- `failed_last_24h:464` are historical rows from the 401 window; they stay `failed` (no auto-retry) but their entities were re-synced by reconcile. Will decay out of the 24h window.
- Spec check #6's original `GROUP BY entity_id` query is too coarse (flags legitimate lifecycle/replay traffic). The accurate PR-A signal is duplicate `event_id` — used here.
- ARCH-010 (non-deterministic outbox `event_id`) remains open; it doesn't affect dedup correctness (InfraSafe `isDuplicateEvent` + per-send event_id), but a deterministic `event_id` would make receiver-side dedup cheaper.
