# Тестовые данные на profk.uz — прод-проверка модуля «Жители» (2026-07-28/29)

Создано мной при проверке раздела в проде после каждой выкатки.
**Ничего не удалено** — удаление только по отдельному подтверждению владельца.

## Что осталось в БД (сверено запросом 2026-07-29)

| Таблица | Строк | Признак |
|---|---|---|
| `users` | 1 | `id = 9`, `telegram_id = 999000001` |
| `user_apartments` | 3 | `user_id = 9` |
| `user_verifications` | 1 | `user_id = 9` |
| `user_documents` | 0 | удалены самим сценарием верификации |
| `audit_logs` | 14 | `telegram_user_id = 999000001` |

## Учётная запись

| Поле | Значение |
|---|---|
| ФИО | `ТЕСТ-QA Жителев` |
| `users.id` | `9` |
| `telegram_id` | `999000001` |
| username | `qa_resident_delete_me` |
| телефон | `+998900000001` |
| роли | `["applicant"]` |
| статус / верификация | `approved` / `verified` |

`telegram_id` выбран **заведомо несуществующим**: это и защита от рассылки
чужому человеку, и попутная проверка деградации — уведомления обязаны падать
молча, не роняя запрос менеджера. Так и вышло: все мутации отвечали 200/201.

## Зависимые строки

| Таблица | Что именно |
|---|---|
| `user_apartments` | привязки к квартирам `id 10` (`ua=5`), `id 12` (`ua=6`, основная), `id 13` (`ua=7`, авто-одобрена при верификации). Привязка к квартире `id 1` (`ua=4`) создавалась и была удалена ходом проверки |
| `user_verifications` | одна запись (`id=1`, `APPROVED`) — она переиспользовалась при повторных запросах документов, а не плодилась |
| `user_documents` | одна запись создавалась SQL-ом (`QA_FAKE_FILE_ID_DELETE_ME`) и **удалена сценарием верификации** — ровно то поведение, которое проверялось |
| `audit_logs` | 14 записей: `user_approved`, `user_blocked`, `user_unblocked`, `resident_apartment_attached`, `resident_binding_approved`, `resident_binding_updated`, `resident_binding_removed`, `resident_documents_requested`, `resident_verification_approved` |

**Квартиры, дома и дворы не создавались** — использованы существующие,
их трогать нельзя.

## Готовый SQL для зачистки

Выполнять под `uk_admin` (роль `profk_bot` не видит часть таблиц). Порядок
важен: сначала зависимые строки, потом сам пользователь.

```sql
BEGIN;
DELETE FROM user_documents     WHERE user_id = 9;
DELETE FROM user_verifications WHERE user_id = 9;
DELETE FROM user_apartments    WHERE user_id = 9;
DELETE FROM audit_logs         WHERE telegram_user_id = 999000001;
DELETE FROM users              WHERE id = 9 AND telegram_id = 999000001;
COMMIT;
```

Перед `COMMIT` стоит убедиться, что затронуто ожидаемое число строк —
`id = 9` проверяется вместе с `telegram_id`, чтобы промах по id не удалил
живого человека.

## Временные изменения, УЖЕ откаченные

* `users.roles` временно менялись на `["applicant","executor"]` для проверки
  запрета блокировки мультиролевых (Т2) — возвращены к `["applicant"]`.
* `users.verification_status` временно возвращался в `requested`, чтобы снять
  скриншот диалога подтверждения личности — возвращён в `verified`.

**2026-07-29, прод-проверка аудита (PR #299/#300).** У жителя `id=9` временно
менялись:

| Поле | Временное значение | Зачем | Восстановлено в |
|---|---|---|---|
| `roles` | `["applicant","executor"]` | guard Т2 на одобрении: `approve` и `block` → 409 | `["applicant"]` |
| `status` | `pending` | увидеть кнопку «Одобрить» и её исчезновение при стафф-роли | `approved` |

Сверено после отката: `9 | approved | ["applicant"] | verified | applicant` —
совпадает с состоянием до правок. Записей в `audit_logs` не прибавилось
(осталось 14): guard отклоняет операцию **до** мутации, поэтому следа она не
оставляет.

Текущее состояние в БД соответствует последнему реальному сценарию.

## Чужие данные — не трогались

Живые аккаунты (`Nazya`, `ULUGBEK`, `Mikhail Grafov`, `Администратор`)
читались, но **не изменялись**. Единственные обращения по чужому id
(`residents/8`) были намеренными проверками ownership и вернули 404.

## Что НЕ создавалось

* Файлы в Media Service и сообщения в TG-каналах — документы через бота не
  загружались (нужна живая Telegram-сессия жителя), запись документа заведена
  напрямую SQL-ом с фиктивным `file_id`.
* Записи на infrasafe.uz — туда ничего не деплоилось и не писалось.
* `webhook_outbox` — у событий `apartment_request.*` в `_ROUTING` endpoint=None,
  строка в outbox не создаётся вовсе; чистить нечего.
