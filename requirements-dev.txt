# Development dependencies
# A9-P2-19: ЕДИНСТВЕННЫЙ источник версий тест-инструментов (pytest*, coverage) —
# в runtime requirements.in их нет. pytest-mock удалён: фикстура mocker не
# используется нигде.
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
# A9-P2-22 (решение владельца 2026-09-23): black и isort удалены — объявлены в
# pre-commit, но не соблюдались (~718 файлов к переформатированию, 1105 нарушений
# порядка импортов). Стиль держит только `ruff check` (E+F, CI-гейт); массового
# переформатирования нет.
ruff==0.15.17
mypy==1.7.1
pre-commit==3.6.0
coverage==7.3.2
aiosqlite==0.20.0
# AUD5-PRAC-9 / AUD5-DEP-1 (2026-07-26): удалены `flake8==6.1.0` (полностью
# вытеснен ruff — те же E+F, и в CI гейтом стоит именно ruff; в pre-commit
# flake8 не был подключён ни разу), `pytest-html==4.1.1` и
# `pytest-xdist==3.5.0` — ни одного упоминания `--html`/`-n auto` в CI,
# Makefile, pytest-конфиге или скриптах.
