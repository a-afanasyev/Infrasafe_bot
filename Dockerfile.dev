# UK Management Bot - Development Dockerfile
# Этот Dockerfile предназначен для разработки с hot-reload
# Код монтируется как volume, изменения применяются без пересборки

FROM python:3.11-slim

# Устанавливаем рабочую директорию
WORKDIR /app

# Устанавливаем системные зависимости
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

# Копируем только requirements для установки зависимостей
COPY requirements.txt .
COPY uk_management_bot/requirements.txt ./uk_management_bot/

# Устанавливаем Python зависимости
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir -r uk_management_bot/requirements.txt

# Устанавливаем переменные окружения для разработки
ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1
ENV LOG_LEVEL=DEBUG
ENV DEBUG=true

# Создаем пользователя для безопасности
RUN useradd --create-home --shell /bin/bash app && \
    chown -R app:app /app

# Переключаемся на пользователя app
USER app

# Открываем порт для health check
EXPOSE 8000

# Команда запуска для разработки
CMD ["python", "uk_management_bot/main.py"]
