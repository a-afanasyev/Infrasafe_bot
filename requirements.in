# Telegram Bot Framework
aiogram>=3.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0
asyncpg>=0.29.0  # Async PostgreSQL driver for SQLAlchemy

# Redis for rate limiting and caching
redis>=5.0.0

# Configuration and Environment
python-dotenv>=1.0.0

# Task Scheduling
apscheduler>=3.10.0

# Development tools
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Async HTTP client (for Media Service integration)
httpx>=0.25.0

# Web Framework for invite registration
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# JWT + Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0  # DEAD-11: код использует `import bcrypt` напрямую, passlib не нужен

# Rate limiting
slowapi>=0.1.9

# Email validation for pydantic EmailStr (SEC-05)
email-validator>=2.0.0

# Prometheus /metrics exposition (OPS-105: outbox observability)
prometheus-client>=0.20.0

# Error Tracking
sentry-sdk[fastapi]>=2.0.0

# Testing
pytest-cov>=4.1.0

# Transitive deps that aren't auto-resolved by uv pip compile (env markers etc)
greenlet>=3.0.0  # sqlalchemy async needs it on darwin/linux x86_64/arm64
