{
  // A9-P3-22: превью-харнесс экранов доступа (источник docs/access-control/guides/img)
  // проверяется `tsc -b` (reference в tsconfig.json) → в CI через `npm run build`.
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.preview.tsbuildinfo"
  },
  "include": ["preview"]
}
