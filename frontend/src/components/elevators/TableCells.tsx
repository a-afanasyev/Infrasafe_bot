/**
 * Ячейки таблиц модуля «Лифты».
 *
 * Разметка переехала в `components/shared/TableParts.tsx` — она понадобилась
 * и другим разделам. Здесь остался реэкспорт, чтобы не трогать пять
 * потребителей внутри модуля.
 */
export { Th, Td, TableShell, HeadRow, BodyRow } from '../shared/TableParts'
