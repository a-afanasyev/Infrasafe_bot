"""Слой репозиториев access_control: stateless query-хелперы доступа к данным.

Репозитории инкапсулируют SQL/ORM для сущностей домена и принимают активную
``Session``/``Connection`` от сервисного слоя. Они НЕ управляют транзакцией: не
делают ``commit``/``rollback``, не берут advisory-lock и не определяют порядок
операций — это остаётся в сервисах (``ingestion``/``lifecycle``). Hash-chain
append-only таблиц (``next_hash`` ПЕРЕД INSERT, §9.7) выполняется здесь как часть
самой вставки, семантически идентично прежнему инлайновому коду.
"""
